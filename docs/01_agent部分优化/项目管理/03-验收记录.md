# 验收记录

> 本文件只追加，不改写历史结果。详细输出保存在对应 handoff、CI 或 exact-HEAD 证据目录。

## 记录

### ACC-20260721-001 Python 3.12 与测试目录统一

| 项目 | 结果 |
|---|---|
| 提交 | `b97c9f9` |
| 结论 | 通过 |
| 证据 | 非集成 `1549 passed, 362 deselected`；专项 `157 passed`；Ruff、mypy 154 files、lock、diff check 通过 |
| 范围 | Python 固定 3.12；`test_agent/test_l0_1_contract.py` 归并到 `tests/` |

### ACC-20260721-002 L4.5-11-0 任务发布核对

| 项目 | 结果 |
|---|---|
| 提交 | `6a7b8c4` |
| 结论 | 发布成立，不是任务验收通过 |
| 证据 | L0 文档契约 `131 passed`；只修改 docs；任务、旧方案和范围记录全部纳入 Git |
| 状态 | L4.5-11-0 已发布 / 待交付 |

### ACC-20260721-003 项目管理文档重构

| 项目 | 结果 |
|---|---|
| 基线 | `6a7b8c4` |
| 提交 | 与本记录同一提交 |
| 结论 | 通过 |
| 结构 | 一个兼容入口、五本职责台账、文档索引、两份模板和只读历史目录 |
| 链接 | 管理中心 13 个 Markdown 文件检查，断链 0 |
| 契约 | L0 文档契约 `131 passed` |
| 范围 | 只有 docs 与 `.gitignore`；无生产代码、测试、依赖或运行配置变化 |
| 跟踪 | 修复根目录和 `docs/.gitignore` 双层忽略；历史 handoff、L5 预审和后续台账纳入 Git |
| 结果 | 当前任务仍为 L4.5-11-0 已发布 / 待交付 |

### ACC-20260721-004 L4.5-11-0 v2 方案第 1 轮验收

| 项目 | 结果 |
|---|---|
| 任务发布基线 | `a1b80ef` |
| 交付提交 | `5280d3a963d3e6addedd3f22ca279d55876f730c` |
| 结论 | **未通过 / 限定返工** |
| 范围核对 | 通过；只新增 v2 方案和 handoff 两份 tracked 文档，无代码、测试、依赖、配置或 PM 台账改动 |
| 自动化证据 | Context 收集 9 项；L0 文档契约 `131 passed in 2.01s`；`git diff --check` 通过；验收开始工作区 clean |
| P1-1 | `[REDACTED:11]`、`[REDACTED:18]`、`[REDACTED:2]` 实际长度为 13、13、12；NFKC 也可能扩展字符，EvidenceSpan offset 不变结论不成立 |
| P1-2 | `_call_gateway()` 不是最终 HTTP payload 构造点；Gateway 内 fallback 不会重进该方法，逐 message 扫描也不覆盖跨 message 重组，且共享 Runtime 影响陈述矛盾 |
| P1-3 | 临床数字在当前代码上已经保持不变，L4.5-11-3 没有先红证据，并与 L4.5-11-1 的 `context.py` / 临床保护范围重叠 |
| P2 | `ActiveObservationContext` 的 `Any` 值、姓名保护来源和结构化身份边界描述未闭合 |
| 阻塞变化 | AR-B-031 保持 P1 打开；新增 R-GATE-001，更新 R-GROUND-001 |
| 下一动作 | 发布 `L4.5-11-0-R1`，只修订同两份文档；不发布任何实现任务 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-0.md` §12 |

### ACC-20260722-005 L4.5-11-0 v2.2 PM 接管后重新验收

| 项目 | 结果 |
|---|---|
| R1 交付提交 | `30f919e94eb4e1da4599d790699efe003736cab3` |
| R1 重新验收 | **仍未通过**：fallback/retry 事实、Intake-only 作用域、跨 message 坐标、宽泛数字白名单残留和任务 3 所有权仍未闭合 |
| 接管授权 | 项目 owner 要求“若还不通过，直接接手完成”；PM 未发布 R2，直接修订两份方案交付文档 |
| 验收目标提交 | `22feb390425c3b4fc4349980566a5c80314e60d0` |
| 最终结论 | **通过（PM 接管修订后）** |
| 范围核对 | `22feb39` 相对 `30f919e` 只修改 v2 方案与 handoff；无生产代码、测试、依赖、配置或 PM 台账修改；目标工作区 clean |
| 代码事实 | Runtime 对真实 Gateway 传 `max_requests=1`；目标 Intake 路径不进入 JSON fallback，传输请求也不扩张；Legacy/其他 Agent 明确非目标 |
| 方案核对 | 有限手机号/身份证号 grammar；全角逐字符坐标；跨 message `B`；raw-source 等长回写；临床六类负向不变量；固定脱敏失败码 |
| 候选任务 | 只保留 L4.5-11-1 入口 projector/scanner 和 L4.5-11-2 Runtime 发送前门禁；生产职责、专项测试文件和依赖互斥 |
| 真实先红 | 当前 USER 层手机号原值可复现；直接绕过入口给 Runtime 不安全 Intake messages 时 fake Gateway 当前调用 1 次 |
| 自动化证据 | 方案结构断言通过；L0 文档契约 `131 passed in 2.05s`；`git diff --check 30f919e..22feb39` 通过 |
| 事实复核边界 | PM 在接管提交后按 exact commit 分阶段复核；由于 PM 同时承担修订，不声称存在额外独立 reviewer，也不替代 L4.5-11 实现后的独立复审 |
| 阻塞变化 | AR-B-031、R-GROUND-001、R-GATE-001 保持打开，处置更新为 v2.2 两个实现任务 |
| 状态迁移 | L4.5-11-0 → 已完成；L4.5-11-0-R1 → 原交付未通过、PM 接管后关闭；L4.5-11 → 方案通过/实现未开始 |
| 下一动作 | 只发布 `L4.5-11-1 Intake 入口投影层`；当前验收不授权直接实施 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-0.md` §14～§15 |

### ACC-20260722-006 L4.5-11-1 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `a00e5b3917a5d3ae38fe37aa091384eca66f99e4`，工作区 clean |
| 方案/生产基线 | v2.2 方案 `22feb39`；生产代码仍为 `b97c9f9` |
| 前置条件 | `ACC-20260722-005` 已通过；`AR-B-031` 保持 P1 打开 |
| 结论 | **L4.5-11-1 已发布 / 待交付；不是实现验收通过** |
| 真实先红 | 单消息手机号仍原样进入 USER；号码拆为两条 current messages 后两个片段仍原样进入 USER |
| 合同范围 | 只允许 `context.py`、`intake_extraction.py`、唯一新增专项测试和唯一新增 handoff；Runtime/Gateway/schema/verifier/现有测试/PM 台账均禁止 |
| 有限模型 | ASCII/全角 digit/X、三种精确手机号分隔符、跨 message `B`、raw 坐标等长 `█`；临床六类负向不变量；不含姓名/其他证件/任意编码 |
| 基线相关回归 | 显式 `-m "not integration"` 后 `65 passed, 22 deselected` |
| 基线静态/L0/全量 | Ruff 通过；mypy 2 files 通过；L0 文档契约 `131 passed`；全量非集成 `1549 passed, 362 deselected` |
| 命令勘误 | 清空 pytest `addopts` 却不显式加 marker 会混跑 unit/integration 并被隔离钩子拒绝；任务合同已固定为 `-m "not integration"` |
| 管理校验适配 | 通用 skill 校验器要求英文固定文件名且禁止跨管理目录链接，不适配本项目既有中文六账本；不创建平行台账，改用六账本存在性、仓库内相对链接、任务状态与 L0 契约校验 |
| 状态迁移 | L4.5-11-1：planned → published；L4.5-11：方案通过/实现未开始 → 实现中 |
| 下一动作 | 执行者从包含任务合同的 clean exact HEAD 先红后绿实施并提交 `agent-refactor-l4-5-11-1.md`；L4.5-11-2 继续 planned |

### ACC-20260722-007 L4.5-11-1 第 1 轮验收

| 项目 | 结果 |
|---|---|
| 发布合同提交 | `c8414bbefabd80fb9e308da4294a211c87ab6e02` |
| 交付提交 | `ca1c34b0bafbb22b3ba68d92ef4122717b400818`；父提交为发布合同；验收开始工作区 clean |
| 结论 | **未通过 / 限定返工** |
| 范围核对 | 通过；只修改合同允许的 4 个文件；正式代码、专项测试和 handoff 均 tracked；`git diff --check` 通过 |
| 已通过门禁 | 专项 `43 passed in 2.32s`；相关 `65 passed, 22 deselected in 2.62s`；Ruff、mypy 2 files、L0 `131 passed in 2.53s` 均通过 |
| P1-1 | `13812345678901234X` 的较短手机号候选被边界拒绝后仍推进游标，同起点 18 位身份证未参与选择；身份证在第 17 位后跨 message 到末位 `X` 也未命中。两例 scanner 均为 `False`、遮罩数均为 0 |
| 测试缺口 | “身份证每个切分位置”测试只在 `total_masked > 0` 时断言，零命中静默通过，没有证明任务合同 §10.4 |
| P1-2 | 公共 helper 只净化 token 化异常；matcher/mask 异常可直接抛含输入的 `ValueError`。故障注入实际得到 `ValueError` 且固定虚构原值可见 |
| P2 | handoff 的交付 exact HEAD 仍为“提交后填写”，交付证据不完整 |
| 全量说明 | 交付声称 `1592 passed, 362 deselected`；PM 因关键 P1 探针已失败未重复运行，不将交付方声明作为本轮通过证据；R1 后必须重跑 |
| 阻塞变化 | AR-B-031 保持打开；新增 R-MATCH-001、R-FAILCLOSED-001；L4.5-11-2 保持 planned |
| 状态迁移 | L4.5-11-1：已交付 → 返工中；L4.5-11-1-R1：未开始 → 已发布 / 待交付 |
| 下一动作 | 只执行 `agent-refactor-l4-5-11-1-rework-1-task.md`，先红后绿修复 matcher 与完整 fail-closed 边界 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-1.md` §11 |

### ACC-20260722-008 L4.5-11-1-R1 重新验收

| 项目 | 结果 |
|---|---|
| R1 合同提交 | `508980d9e9d8f6073811a689d85694fb91d49390` |
| R1 交付提交 | `140262d944e16cc6043124d7537794bb46cd7960`；父提交精确为 R1 合同提交；验收前后 worktree clean |
| 结论 | **通过 / accepted**；L4.5-11-1-R1 关闭，L4.5-11-1 完成 |
| 范围核对 | 只修改 R1 允许的 `context.py`、唯一专项测试和原 handoff；全部 tracked；未修改 Intake 集成、PM 台账或合同禁区；`git diff --check` 通过 |
| 独立 Review | No findings（P0/P1/P2/P3 均无）；独立复验同起点候选、68 个身份证单一切分、连续多 `B`、完整 fail-closed、逐 message 等长、DTO/grounding 不变量 |
| 独立 CI | 专项 `53 passed`；相关 `65 passed, 22 deselected`；Ruff、mypy 2 files、L0 `131 passed`、全量非集成 `1602 passed, 362 deselected` 全部通过 |
| PM 探针 | 4 个同起点 ASCII/全角 `X/x` 变体、68 个单一切分、连续多 `B`、3 个 matcher/mask 故障注入全部通过；固定错误无 cause/context/log 原值 |
| 失败史 | `ACC-20260722-007` 和 `ca1c34b` 第 1 轮失败完整保留；未删除、覆盖或降级失败证据 |
| 风险变化 | R-MATCH-001、R-FAILCLOSED-001 关闭；R-GROUND-001 获得实现证据并保留组合回归；AR-B-031、R-GATE-001 继续打开 |
| 下一动作 | 项目经理以 accepted baseline 设计并发布 bounded L4.5-11-2；发布提交前禁止实施 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-1.md` §12～§13 |

### ACC-20260722-009 L4.5-11-2 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `2f008b9ae1bf231abe4fd3fdea965ec665922eff`，L4.5-11-1 acceptance 管理提交，工作区 clean |
| 前置验收 | `ACC-20260722-008`：L4.5-11-1 accepted；accepted 生产提交 `140262d`；方案 `22feb39` |
| 结论 | **L4.5-11-2 已发布 / 待交付；不是实现验收通过** |
| 真实先红 | 未投影 unsafe Intake messages 直接进入 Runtime 时当前成功返回；fake Gateway 方法调用 1 次、actual request 1 次；`MODEL_INPUT_PRIVACY_VIOLATION` 不存在 |
| 调用链核对 | `_call_gateway()` 在 Gateway 方法选择前持有 agent name 和完整 final messages；真实方法接收 `max_requests=1`；recorder 只记录 digest/元数据/固定错误码 |
| 合同范围 | 只允许 `runtime.py`、`specs.py`、唯一新增专项测试和唯一新增 handoff；accepted scanner、Intake、Gateway、既有测试、PM 台账均禁止修改 |
| 作用域 | 只以现有 `INTAKE_AGENT_NAME` 精确启用；只扫描 final message 字符串 content；非 Intake、Legacy、直接 Gateway 明确不覆盖 |
| 基线门禁 | Runtime/Agent 相关 `214 passed, 22 deselected`；L4.5-11-1 专项 `53 passed`；Ruff、mypy 2 files、L0 `131 passed`；最近独立 CI 全量 `1602 passed, 362 deselected` |
| 发布提交 | 与本记录、任务合同和管理状态同一独立文档提交；执行者必须从包含合同的 clean exact HEAD 开始 |
| 下一动作 | 执行者按 `agent-refactor-l4-5-11-2-task.md` 先红后绿交付；其他任务冻结 |

### ACC-20260722-010 L4.5-11-2 实现验收

| 项目 | 结果 |
|---|---|
| 发布提交 | `2da64ae64ddbd22cd9631e72ea37eaf9f257b36e` |
| 交付提交 | `5ada2622255b3c9dc25649ec391f45b35123559a`；父提交精确为发布提交；验收前后 worktree clean |
| 结论 | **通过 / accepted**；L4.5-11-2 完成 |
| 真实先红 | 生产代码仍为发布行为时专项 `16 failed, 3 passed`；observed/plain unsafe 用例各调用 Gateway 1 次、request 1 次，无固定错误码；无 collection/import 替代失败 |
| 范围核对 | 只修改 `runtime.py`、`specs.py`，新增唯一专项测试和 handoff；新增文件 tracked mode `100644`；未修改 accepted scanner、Intake、Gateway、Legacy、既有测试或 PM 台账 |
| 独立 Review | No findings（P0/P1/P2/P3 均无）；证明 guard 先于 Gateway property/method lookup、只扫描 content tuple、完整 fixed/chainless fail-closed、零调用、不可重试、安全/非 Intake 不变量 |
| 独立 CI | 任务 2 专项 `19 passed`；任务 1 专项 `53 passed`；相关 `267 passed, 22 deselected`；Ruff、mypy 2 files、L0 `131 passed`、全量非集成 `1621 passed, 362 deselected` 全部通过 |
| PM 探针 | 跨 final message 与 scanner 故障零调用、固定错误、安全 Intake 单次 `max_requests=1`、非 Intake `_call_gateway()` 不读 content 全部通过 |
| Writer 转移 | 原 writer 从 clean release HEAD 只留下同任务测试草稿；PM 中断后核对唯一性/范围并明确转交替代 writer；无用户或无关修改混入 |
| 风险变化 | R-GATE-001 关闭；AR-B-031 保持打开，等待组合关闭验收；L4.5/L5 既有 NO-GO 不变 |
| 下一动作 | 在包含本验收记录的 clean exact HEAD 上执行 L4.5-11 组合关闭验收和最终未参与实现 Reviewer 复审 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-2.md` §1～§9 |

### ACC-20260722-011 L4.5-11 组合关闭验收第 1 轮

| 项目 | 结果 |
|---|---|
| 冻结 exact HEAD | `bdf155fb0ed0c0ce37ea0e6c75b7afac6f1aa6ef`；L4.5-11-2 acceptance 管理提交；复审前后 worktree clean |
| 结论 | **未通过 / 限定返工**；AR-B-031 保持打开 |
| 独立 CI | 两专项 `72 passed`；七文件相关 `267 passed, 22 deselected`；Ruff 6 files、mypy 4 production files、L0 `131 passed`、全量非集成 `1621 passed, 362 deselected`；范围/tracked/diff/exact/clean 全部通过 |
| 通过的组合证据 | 原始 DTO/持久化/grounding 所有权不变；入口逐坐标等长投影；支持集合完整；Runtime 绕过零 Gateway 调用；safe Intake `max_requests=1`；非 Intake/Legacy/临床数字/公开开关不变；正式合同、测试、handoff 和六账本 tracked |
| 独立 Review | P0/P1/P3 无；P2 1 个：4,000 位合法数字消息 scanner `22.57 ms/message`、projector `23.23 ms/message`，8-message 约 `184.80/182.85 ms`，触发 v2.2 R4 `>10 ms/message` |
| PM 独立复现 | Python 3.12 / `uv run`，10 次预热、50 次采样：safe `1.689/1.571 ms`；digit-dense scanner/projector 中位数 `24.161/24.748 ms`，确认非偶发噪声 |
| 跨层 PM 探针 | DTO 不变、跨 message 等长、临床 offset 不变；投影后 Intake 请求 1、直接 unsafe Intake 请求 0、非 Intake 请求 1，全部通过 |
| 风险变化 | 新增 R-PERF-001；R-GROUND-001、R-GATE-001 等既有正确性证据不回退；性能 finding 未处置前不得关闭 AR-B-031 |
| 下一动作 | 只执行 L4.5-11-1-R2 matcher 线性性能限定返工；不重新定义 SLA，不进入 L5 |

### ACC-20260722-012 L4.5-11-1-R2 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `bdf155fb0ed0c0ce37ea0e6c75b7afac6f1aa6ef`，工作区 clean |
| 结论 | **已发布 / 待交付；不是返工验收通过** |
| 合同范围 | 只允许 `context.py`、L4.5-11-1 原专项测试和新 R2 handoff；Runtime、Intake 集成、task2 测试、Legacy、临床、Domain、管理台账均禁止修改 |
| 真实先红 | Reviewer 与 PM 已双重复现；执行者仍必须在未改生产代码时以正式专项性能断言得到 RED |
| 验收阈值 | 单条 4,000 位数字 scanner/projector 均 `<10 ms/message`；8-message batch 均 `<80 ms`；不得缩短输入、跳过扫描、改 grammar 或上调阈值 |
| 管理校验适配 | 继续使用既有中文六账本、仓库内相对链接、状态一致性与 L0；不创建通用 skill 所期待的平行英文台账 |
| 下一动作 | 项目经理创建独立发布提交；唯一开发 writer 从该 clean exact HEAD 先红后绿交付 |

## 新记录模板

```markdown
### ACC-YYYYMMDD-NNN 任务名

| 项目 | 结果 |
|---|---|
| 基线/提交 |  |
| 结论 | 通过 / 未通过 / 阻塞 |
| 范围核对 |  |
| 专项测试 |  |
| 全量门禁 |  |
| 独立复审 |  |
| 阻塞变化 |  |
| 下一动作 |  |
```
