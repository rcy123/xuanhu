# 验收记录

> 本文件只追加，不改写历史结果。详细输出保存在对应 handoff、CI 或 exact-HEAD 证据目录。

## 记录

### ACC-20260721-001 Python 3.12 与测试目录统一

| 项目 | 结果 |
|---|---|
| 提交 | `b97c9f9` |
| 结论 | 通过 |
| 证据 | 非集成 `1549 passed, 362 deselected`；专项 `157 passed`；Ruff、mypy 154 files、lock、diff check 通过 |
| 范围 | Python 固定 3.12；`test_agent/test_l0_1_contract.py` 归并到 `tests/` |

### ACC-20260721-002 L4.5-11-0 任务发布核对

| 项目 | 结果 |
|---|---|
| 提交 | `6a7b8c4` |
| 结论 | 发布成立，不是任务验收通过 |
| 证据 | L0 文档契约 `131 passed`；只修改 docs；任务、旧方案和范围记录全部纳入 Git |
| 状态 | L4.5-11-0 已发布 / 待交付 |

### ACC-20260721-003 项目管理文档重构

| 项目 | 结果 |
|---|---|
| 基线 | `6a7b8c4` |
| 提交 | 与本记录同一提交 |
| 结论 | 通过 |
| 结构 | 一个兼容入口、五本职责台账、文档索引、两份模板和只读历史目录 |
| 链接 | 管理中心 13 个 Markdown 文件检查，断链 0 |
| 契约 | L0 文档契约 `131 passed` |
| 范围 | 只有 docs 与 `.gitignore`；无生产代码、测试、依赖或运行配置变化 |
| 跟踪 | 修复根目录和 `docs/.gitignore` 双层忽略；历史 handoff、L5 预审和后续台账纳入 Git |
| 结果 | 当前任务仍为 L4.5-11-0 已发布 / 待交付 |

### ACC-20260721-004 L4.5-11-0 v2 方案第 1 轮验收

| 项目 | 结果 |
|---|---|
| 任务发布基线 | `a1b80ef` |
| 交付提交 | `5280d3a963d3e6addedd3f22ca279d55876f730c` |
| 结论 | **未通过 / 限定返工** |
| 范围核对 | 通过；只新增 v2 方案和 handoff 两份 tracked 文档，无代码、测试、依赖、配置或 PM 台账改动 |
| 自动化证据 | Context 收集 9 项；L0 文档契约 `131 passed in 2.01s`；`git diff --check` 通过；验收开始工作区 clean |
| P1-1 | `[REDACTED:11]`、`[REDACTED:18]`、`[REDACTED:2]` 实际长度为 13、13、12；NFKC 也可能扩展字符，EvidenceSpan offset 不变结论不成立 |
| P1-2 | `_call_gateway()` 不是最终 HTTP payload 构造点；Gateway 内 fallback 不会重进该方法，逐 message 扫描也不覆盖跨 message 重组，且共享 Runtime 影响陈述矛盾 |
| P1-3 | 临床数字在当前代码上已经保持不变，L4.5-11-3 没有先红证据，并与 L4.5-11-1 的 `context.py` / 临床保护范围重叠 |
| P2 | `ActiveObservationContext` 的 `Any` 值、姓名保护来源和结构化身份边界描述未闭合 |
| 阻塞变化 | AR-B-031 保持 P1 打开；新增 R-GATE-001，更新 R-GROUND-001 |
| 下一动作 | 发布 `L4.5-11-0-R1`，只修订同两份文档；不发布任何实现任务 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-0.md` §12 |

### ACC-20260722-005 L4.5-11-0 v2.2 PM 接管后重新验收

| 项目 | 结果 |
|---|---|
| R1 交付提交 | `30f919e94eb4e1da4599d790699efe003736cab3` |
| R1 重新验收 | **仍未通过**：fallback/retry 事实、Intake-only 作用域、跨 message 坐标、宽泛数字白名单残留和任务 3 所有权仍未闭合 |
| 接管授权 | 项目 owner 要求“若还不通过，直接接手完成”；PM 未发布 R2，直接修订两份方案交付文档 |
| 验收目标提交 | `22feb390425c3b4fc4349980566a5c80314e60d0` |
| 最终结论 | **通过（PM 接管修订后）** |
| 范围核对 | `22feb39` 相对 `30f919e` 只修改 v2 方案与 handoff；无生产代码、测试、依赖、配置或 PM 台账修改；目标工作区 clean |
| 代码事实 | Runtime 对真实 Gateway 传 `max_requests=1`；目标 Intake 路径不进入 JSON fallback，传输请求也不扩张；Legacy/其他 Agent 明确非目标 |
| 方案核对 | 有限手机号/身份证号 grammar；全角逐字符坐标；跨 message `B`；raw-source 等长回写；临床六类负向不变量；固定脱敏失败码 |
| 候选任务 | 只保留 L4.5-11-1 入口 projector/scanner 和 L4.5-11-2 Runtime 发送前门禁；生产职责、专项测试文件和依赖互斥 |
| 真实先红 | 当前 USER 层手机号原值可复现；直接绕过入口给 Runtime 不安全 Intake messages 时 fake Gateway 当前调用 1 次 |
| 自动化证据 | 方案结构断言通过；L0 文档契约 `131 passed in 2.05s`；`git diff --check 30f919e..22feb39` 通过 |
| 事实复核边界 | PM 在接管提交后按 exact commit 分阶段复核；由于 PM 同时承担修订，不声称存在额外独立 reviewer，也不替代 L4.5-11 实现后的独立复审 |
| 阻塞变化 | AR-B-031、R-GROUND-001、R-GATE-001 保持打开，处置更新为 v2.2 两个实现任务 |
| 状态迁移 | L4.5-11-0 → 已完成；L4.5-11-0-R1 → 原交付未通过、PM 接管后关闭；L4.5-11 → 方案通过/实现未开始 |
| 下一动作 | 只发布 `L4.5-11-1 Intake 入口投影层`；当前验收不授权直接实施 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-0.md` §14～§15 |

### ACC-20260722-006 L4.5-11-1 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `a00e5b3917a5d3ae38fe37aa091384eca66f99e4`，工作区 clean |
| 方案/生产基线 | v2.2 方案 `22feb39`；生产代码仍为 `b97c9f9` |
| 前置条件 | `ACC-20260722-005` 已通过；`AR-B-031` 保持 P1 打开 |
| 结论 | **L4.5-11-1 已发布 / 待交付；不是实现验收通过** |
| 真实先红 | 单消息手机号仍原样进入 USER；号码拆为两条 current messages 后两个片段仍原样进入 USER |
| 合同范围 | 只允许 `context.py`、`intake_extraction.py`、唯一新增专项测试和唯一新增 handoff；Runtime/Gateway/schema/verifier/现有测试/PM 台账均禁止 |
| 有限模型 | ASCII/全角 digit/X、三种精确手机号分隔符、跨 message `B`、raw 坐标等长 `█`；临床六类负向不变量；不含姓名/其他证件/任意编码 |
| 基线相关回归 | 显式 `-m "not integration"` 后 `65 passed, 22 deselected` |
| 基线静态/L0/全量 | Ruff 通过；mypy 2 files 通过；L0 文档契约 `131 passed`；全量非集成 `1549 passed, 362 deselected` |
| 命令勘误 | 清空 pytest `addopts` 却不显式加 marker 会混跑 unit/integration 并被隔离钩子拒绝；任务合同已固定为 `-m "not integration"` |
| 管理校验适配 | 通用 skill 校验器要求英文固定文件名且禁止跨管理目录链接，不适配本项目既有中文六账本；不创建平行台账，改用六账本存在性、仓库内相对链接、任务状态与 L0 契约校验 |
| 状态迁移 | L4.5-11-1：planned → published；L4.5-11：方案通过/实现未开始 → 实现中 |
| 下一动作 | 执行者从包含任务合同的 clean exact HEAD 先红后绿实施并提交 `agent-refactor-l4-5-11-1.md`；L4.5-11-2 继续 planned |

### ACC-20260722-007 L4.5-11-1 第 1 轮验收

| 项目 | 结果 |
|---|---|
| 发布合同提交 | `c8414bbefabd80fb9e308da4294a211c87ab6e02` |
| 交付提交 | `ca1c34b0bafbb22b3ba68d92ef4122717b400818`；父提交为发布合同；验收开始工作区 clean |
| 结论 | **未通过 / 限定返工** |
| 范围核对 | 通过；只修改合同允许的 4 个文件；正式代码、专项测试和 handoff 均 tracked；`git diff --check` 通过 |
| 已通过门禁 | 专项 `43 passed in 2.32s`；相关 `65 passed, 22 deselected in 2.62s`；Ruff、mypy 2 files、L0 `131 passed in 2.53s` 均通过 |
| P1-1 | `13812345678901234X` 的较短手机号候选被边界拒绝后仍推进游标，同起点 18 位身份证未参与选择；身份证在第 17 位后跨 message 到末位 `X` 也未命中。两例 scanner 均为 `False`、遮罩数均为 0 |
| 测试缺口 | “身份证每个切分位置”测试只在 `total_masked > 0` 时断言，零命中静默通过，没有证明任务合同 §10.4 |
| P1-2 | 公共 helper 只净化 token 化异常；matcher/mask 异常可直接抛含输入的 `ValueError`。故障注入实际得到 `ValueError` 且固定虚构原值可见 |
| P2 | handoff 的交付 exact HEAD 仍为“提交后填写”，交付证据不完整 |
| 全量说明 | 交付声称 `1592 passed, 362 deselected`；PM 因关键 P1 探针已失败未重复运行，不将交付方声明作为本轮通过证据；R1 后必须重跑 |
| 阻塞变化 | AR-B-031 保持打开；新增 R-MATCH-001、R-FAILCLOSED-001；L4.5-11-2 保持 planned |
| 状态迁移 | L4.5-11-1：已交付 → 返工中；L4.5-11-1-R1：未开始 → 已发布 / 待交付 |
| 下一动作 | 只执行 `agent-refactor-l4-5-11-1-rework-1-task.md`，先红后绿修复 matcher 与完整 fail-closed 边界 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-1.md` §11 |

### ACC-20260722-008 L4.5-11-1-R1 重新验收

| 项目 | 结果 |
|---|---|
| R1 合同提交 | `508980d9e9d8f6073811a689d85694fb91d49390` |
| R1 交付提交 | `140262d944e16cc6043124d7537794bb46cd7960`；父提交精确为 R1 合同提交；验收前后 worktree clean |
| 结论 | **通过 / accepted**；L4.5-11-1-R1 关闭，L4.5-11-1 完成 |
| 范围核对 | 只修改 R1 允许的 `context.py`、唯一专项测试和原 handoff；全部 tracked；未修改 Intake 集成、PM 台账或合同禁区；`git diff --check` 通过 |
| 独立 Review | No findings（P0/P1/P2/P3 均无）；独立复验同起点候选、68 个身份证单一切分、连续多 `B`、完整 fail-closed、逐 message 等长、DTO/grounding 不变量 |
| 独立 CI | 专项 `53 passed`；相关 `65 passed, 22 deselected`；Ruff、mypy 2 files、L0 `131 passed`、全量非集成 `1602 passed, 362 deselected` 全部通过 |
| PM 探针 | 4 个同起点 ASCII/全角 `X/x` 变体、68 个单一切分、连续多 `B`、3 个 matcher/mask 故障注入全部通过；固定错误无 cause/context/log 原值 |
| 失败史 | `ACC-20260722-007` 和 `ca1c34b` 第 1 轮失败完整保留；未删除、覆盖或降级失败证据 |
| 风险变化 | R-MATCH-001、R-FAILCLOSED-001 关闭；R-GROUND-001 获得实现证据并保留组合回归；AR-B-031、R-GATE-001 继续打开 |
| 下一动作 | 项目经理以 accepted baseline 设计并发布 bounded L4.5-11-2；发布提交前禁止实施 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-1.md` §12～§13 |

### ACC-20260722-009 L4.5-11-2 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `2f008b9ae1bf231abe4fd3fdea965ec665922eff`，L4.5-11-1 acceptance 管理提交，工作区 clean |
| 前置验收 | `ACC-20260722-008`：L4.5-11-1 accepted；accepted 生产提交 `140262d`；方案 `22feb39` |
| 结论 | **L4.5-11-2 已发布 / 待交付；不是实现验收通过** |
| 真实先红 | 未投影 unsafe Intake messages 直接进入 Runtime 时当前成功返回；fake Gateway 方法调用 1 次、actual request 1 次；`MODEL_INPUT_PRIVACY_VIOLATION` 不存在 |
| 调用链核对 | `_call_gateway()` 在 Gateway 方法选择前持有 agent name 和完整 final messages；真实方法接收 `max_requests=1`；recorder 只记录 digest/元数据/固定错误码 |
| 合同范围 | 只允许 `runtime.py`、`specs.py`、唯一新增专项测试和唯一新增 handoff；accepted scanner、Intake、Gateway、既有测试、PM 台账均禁止修改 |
| 作用域 | 只以现有 `INTAKE_AGENT_NAME` 精确启用；只扫描 final message 字符串 content；非 Intake、Legacy、直接 Gateway 明确不覆盖 |
| 基线门禁 | Runtime/Agent 相关 `214 passed, 22 deselected`；L4.5-11-1 专项 `53 passed`；Ruff、mypy 2 files、L0 `131 passed`；最近独立 CI 全量 `1602 passed, 362 deselected` |
| 发布提交 | 与本记录、任务合同和管理状态同一独立文档提交；执行者必须从包含合同的 clean exact HEAD 开始 |
| 下一动作 | 执行者按 `agent-refactor-l4-5-11-2-task.md` 先红后绿交付；其他任务冻结 |

### ACC-20260722-010 L4.5-11-2 实现验收

| 项目 | 结果 |
|---|---|
| 发布提交 | `2da64ae64ddbd22cd9631e72ea37eaf9f257b36e` |
| 交付提交 | `5ada2622255b3c9dc25649ec391f45b35123559a`；父提交精确为发布提交；验收前后 worktree clean |
| 结论 | **通过 / accepted**；L4.5-11-2 完成 |
| 真实先红 | 生产代码仍为发布行为时专项 `16 failed, 3 passed`；observed/plain unsafe 用例各调用 Gateway 1 次、request 1 次，无固定错误码；无 collection/import 替代失败 |
| 范围核对 | 只修改 `runtime.py`、`specs.py`，新增唯一专项测试和 handoff；新增文件 tracked mode `100644`；未修改 accepted scanner、Intake、Gateway、Legacy、既有测试或 PM 台账 |
| 独立 Review | No findings（P0/P1/P2/P3 均无）；证明 guard 先于 Gateway property/method lookup、只扫描 content tuple、完整 fixed/chainless fail-closed、零调用、不可重试、安全/非 Intake 不变量 |
| 独立 CI | 任务 2 专项 `19 passed`；任务 1 专项 `53 passed`；相关 `267 passed, 22 deselected`；Ruff、mypy 2 files、L0 `131 passed`、全量非集成 `1621 passed, 362 deselected` 全部通过 |
| PM 探针 | 跨 final message 与 scanner 故障零调用、固定错误、安全 Intake 单次 `max_requests=1`、非 Intake `_call_gateway()` 不读 content 全部通过 |
| Writer 转移 | 原 writer 从 clean release HEAD 只留下同任务测试草稿；PM 中断后核对唯一性/范围并明确转交替代 writer；无用户或无关修改混入 |
| 风险变化 | R-GATE-001 关闭；AR-B-031 保持打开，等待组合关闭验收；L4.5/L5 既有 NO-GO 不变 |
| 下一动作 | 在包含本验收记录的 clean exact HEAD 上执行 L4.5-11 组合关闭验收和最终未参与实现 Reviewer 复审 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-2.md` §1～§9 |

### ACC-20260722-011 L4.5-11 组合关闭验收第 1 轮

| 项目 | 结果 |
|---|---|
| 冻结 exact HEAD | `bdf155fb0ed0c0ce37ea0e6c75b7afac6f1aa6ef`；L4.5-11-2 acceptance 管理提交；复审前后 worktree clean |
| 结论 | **未通过 / 限定返工**；AR-B-031 保持打开 |
| 独立 CI | 两专项 `72 passed`；七文件相关 `267 passed, 22 deselected`；Ruff 6 files、mypy 4 production files、L0 `131 passed`、全量非集成 `1621 passed, 362 deselected`；范围/tracked/diff/exact/clean 全部通过 |
| 通过的组合证据 | 原始 DTO/持久化/grounding 所有权不变；入口逐坐标等长投影；支持集合完整；Runtime 绕过零 Gateway 调用；safe Intake `max_requests=1`；非 Intake/Legacy/临床数字/公开开关不变；正式合同、测试、handoff 和六账本 tracked |
| 独立 Review | P0/P1/P3 无；P2 1 个：4,000 位合法数字消息 scanner `22.57 ms/message`、projector `23.23 ms/message`，8-message 约 `184.80/182.85 ms`，触发 v2.2 R4 `>10 ms/message` |
| PM 独立复现 | Python 3.12 / `uv run`，10 次预热、50 次采样：safe `1.689/1.571 ms`；digit-dense scanner/projector 中位数 `24.161/24.748 ms`，确认非偶发噪声 |
| 跨层 PM 探针 | DTO 不变、跨 message 等长、临床 offset 不变；投影后 Intake 请求 1、直接 unsafe Intake 请求 0、非 Intake 请求 1，全部通过 |
| 风险变化 | 新增 R-PERF-001；R-GROUND-001、R-GATE-001 等既有正确性证据不回退；性能 finding 未处置前不得关闭 AR-B-031 |
| 下一动作 | 只执行 L4.5-11-1-R2 matcher 线性性能限定返工；不重新定义 SLA，不进入 L5 |

### ACC-20260722-012 L4.5-11-1-R2 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `bdf155fb0ed0c0ce37ea0e6c75b7afac6f1aa6ef`，工作区 clean |
| 结论 | **已发布 / 待交付；不是返工验收通过** |
| 合同范围 | 只允许 `context.py`、L4.5-11-1 原专项测试和新 R2 handoff；Runtime、Intake 集成、task2 测试、Legacy、临床、Domain、管理台账均禁止修改 |
| 真实先红 | Reviewer 与 PM 已双重复现；执行者仍必须在未改生产代码时以正式专项性能断言得到 RED |
| 验收阈值 | 单条 4,000 位数字 scanner/projector 均 `<10 ms/message`；8-message batch 均 `<80 ms`；不得缩短输入、跳过扫描、改 grammar 或上调阈值 |
| 管理校验适配 | 继续使用既有中文六账本、仓库内相对链接、状态一致性与 L0；不创建通用 skill 所期待的平行英文台账 |
| 下一动作 | 项目经理创建独立发布提交；唯一开发 writer 从该 clean exact HEAD 先红后绿交付 |

### ACC-20260722-013 L4.5-11-1-R2 实现验收

| 项目 | 结果 |
|---|---|
| 发布 / 交付 | `b8c553af9361c97683dac94c2d5a34dc5aca5dd2` → `ada23c77cedd4a3e98db5d4e4a5c11328ee4c0e4`；exact parent 通过；初末 clean |
| 结论 | **通过 / accepted**；L4.5-11-1-R2 关闭，L4.5-11-1 恢复 accepted |
| 真实先红 | 开发 parent：单条 `23.385/22.281 ms`、batch `179.682/185.065 ms`；独立 CI parent-source：`23.065/23.479 ms`、batch `200.383/217.482 ms`；正确性断言通过，四项仅性能阈值失败 |
| 范围核对 | 精确修改 `context.py`、task1 原专项测试和新 R2 handoff；未修改 Runtime、Intake、task2 测试、Legacy、临床、Domain 或 PM 台账；新增 handoff tracked；diff check 通过 |
| 独立 Review | P0/P1/P2/P3 无；GREEN 单条 `4.127/3.954 ms`、batch `32.639/35.650 ms`；40,000 API + 2,168 定向 + 20,000 raw-range 差分通过；O(n)、B/HARD、grammar、fail-closed 与 Runtime 关键不变量通过 |
| 独立 CI | task1 `57 passed`；task2 `19 passed`；相关超集 `313 passed, 48 deselected`；Ruff/mypy；L0 `131 passed`；全量非集成 `1625 passed, 362 deselected`；scope/tracked/exact/clean 全部通过 |
| PM 探针 | 单条 `4.118/3.825 ms`、batch `33.136/31.304 ms`；DTO/等长/clinical offset、安全请求 1、unsafe 请求 0、非 Intake 请求 1 全部通过 |
| 命令校准 | R2 任务书列出的五个旧测试路径不存在；经 PM 授权映射为九个现存语义覆盖文件的 313-test 超集；未改变范围、测试所有权或门槛 |
| 风险变化 | R-PERF-001 获得关闭证据；等待原组合 Reviewer 在同一 exact HEAD 做第 2 轮最终复审 |

### ACC-20260722-014 L4.5-11 最终组合关闭验收

| 项目 | 结果 |
|---|---|
| 组合 exact HEAD | `ada23c77cedd4a3e98db5d4e4a5c11328ee4c0e4`；accepted 生产/测试提交；复审前后 worktree/index clean |
| 结论 | **通过 / accepted**；L4.5-11 完成，AR-B-031 关闭 |
| 最终独立 Review | 最初报告 P2 的 Reviewer 第 2 轮 P0/P1/P2/P3 全无；原 R-PERF-001 resolved；单条 `4.127/3.627 ms`、batch `33.672/32.479 ms`；20,000 组差分通过 |
| 纵深防御 | 原始 DTO/持久化/grounding 所有权不变；入口等长投影完整覆盖支持集合；Runtime direct bypass 与 matcher 故障在任何 Gateway lookup 前固定 chainless 拒绝，lookup/observed/plain/request `0/0/0/0` |
| 不变量 | safe Intake `max_requests=1`；non-Intake 不读 content；Legacy/Gateway/公开开关附加 `45 passed, 2 deselected`；临床、配置、依赖、前端、schema 均未改变 |
| 最终门禁 | 两专项合计 `76 passed`；相关 `313 passed, 48 deselected`；Ruff/mypy；L0 `131 passed`；全量非集成 `1625 passed, 362 deselected`；diff/scope/tracked/exact/clean 全部通过 |
| 失败历史 | `ca1c34b` 第 1 轮正确性失败和 `ACC-20260722-011` 组合第 1 轮 P2 均完整保留；未覆盖、删除或降级 |
| 阻塞与风险 | AR-B-031、R-GROUND-001、R-PERF-001 关闭；有限威胁模型非目标继续保留；EXT-001、EXT-002 未关闭 |
| 状态边界 | L4.5-11 accepted 不等于 L4.5 阶段关闭；L4.5 为工程任务完成 / 外部待签，L5 继续 NO-GO |
| 下一动作 | 仅等待具名临床专业人员完成 L4.5-02 / EXT-001；Codex 不代签、不自动进入 L5 |
| 详细证据 | `docs/dev-handoff/agent-refactor-l4-5-11-1-rework-2.md` §9 |

### ACC-20260722-015 L5-PREP-0 任务发布核对

| 项目 | 结果 |
|---|---|
| 发布输入 exact HEAD | `ad8cbf038cb3e0a18c9ce40f88d5ee235c04a4d7`；分支 `codex/l4-5-11-context-privacy-hardening`；tracked worktree clean |
| 前置工程状态 | L4.5-11 accepted；AR-B-031 关闭；生产代码 accepted baseline 仍为 `ada23c77` |
| 所有者范围决策 | 项目仅用于个人学习、架构实验和软件工程演练；只允许固定虚构、程序生成或无个人属性技术数据 |
| 结论 | **L5-PREP-0 已发布 / 执行中；不是沙盒准入验收通过，也不是 L5-1 实施授权** |
| 合同范围 | 仅管理文档、L5 范围/风险/安全设计文档及本任务 task/handoff；禁止生产代码、测试、依赖、配置、前端和历史记录修改 |
| 外部门禁 | G1～G6、EXT-001、EXT-002 未专业通过；仅对当前沙盒标记 `deferred_for_clinical_use`，真实/公开用途触发时重新打开 |
| 状态边界 | 本状态变化来自项目所有者对用途范围的调整，不是临床、法律、隐私、伦理或监管批准。 |
| 下一动作 | 由唯一 writer 执行 L5-PREP-0；只读子 Agent 提供仓库审计、安全架构与测试设计输入；候选冻结后调用未参与写作的独立 Reviewer |

### ACC-20260722-016 L5-PREP-0 独立 Review 第 1 轮

| 项目 | 结果 |
|---|---|
| 候选基线 | `ad8cbf038cb3e0a18c9ce40f88d5ee235c04a4d7` 上的 13 个授权 Markdown 变更；候选冻结后只读复核 |
| Reviewer 独立性 | 未参与主 Agent 写作，也未参与仓库审计、安全架构或测试设计三个输入流 |
| 结论 | **未通过 / bounded 文档修订**；P0=0、P1=0、P2=1、P3=1 |
| P2 | 准入包和 handoff 对 ignored `.codex_tmp` 的“未见手机号/身份证/邮箱 pattern”表述过度；独立脱敏扫描在生成 requirements/SBOM 中得到 73 phone-like、36 ID-like、99 email-like 命中，虽更像包元数据/误报但未逐项 provenance-aware 分类 |
| P3 | L5-PREP-0 任务书把全部 G1～G6/EXT 状态限制为两类，与沙盒工程部分允许的五类状态不一致 |
| 修订 | 记录脱敏计数、未知性和 `not_admitted` 处置，不复制原值；把“专业门禁部分”和“工程可验证沙盒部分”拆开表述 |
| 失败史 | 本轮 finding 保留，不以最终通过覆盖 |

### ACC-20260722-017 L5-PREP-0 最终验收

| 项目 | 结果 |
|---|---|
| 任务/生产基线 | `ad8cbf038cb3e0a18c9ce40f88d5ee235c04a4d7`；生产代码 accepted baseline 仍为 `ada23c77` |
| 范围核对 | 只修改 13 个任务允许的 Markdown；无生产代码、测试、依赖、配置、前端、data 或历史记录变化 |
| 数据审计 | tracked tree 未确认真实患者数据、生产日志、聊天记录、DB dump 或真实密钥；ignored RAG fixture、`.env`、`.codex_tmp` 均 not admitted，真实外部存储保持 unknown |
| 运行边界 | LangGraph 公共 flag 默认关闭，但无全局 sandbox hard-off；因此只准入离线 adapter，runtime/HTTP/E2E/Compose/DB/RAG/gateway/import/deploy 继续 NO-GO |
| 工程设计 | 用途/限制、触发矩阵、合成数据、L5-1～L5-4 状态机/不变量/失效模式/测试矩阵、风险与免责声明完整 |
| 自动化 | L0 `131 passed`；L0 + seed contract `140 passed`；seed evaluator 29/29、`not_for_clinical_signoff`；Markdown 相对链接和状态一致性、`git diff --check` 通过 |
| 独立 Review | R1 P2/P3 修订后 R2 **PASS**；P0=0、P1=0、P2=0、P3=0 |
| 专业门禁 | G1～G6、EXT-001/EXT-002 未专业通过；当前沙盒 `deferred_for_clinical_use`，触发真实/公开用途后 `external_approval_required` |
| 结论 | **通过 / accepted**；L5 个人学习离线工程沙盒 `sandbox_scope_satisfied` |
| 状态边界 | 本状态变化来自项目所有者对用途范围的调整，不是临床、法律、隐私、伦理或监管批准。 |
| 管理提交 | 本记录、准入包、handoff、台账和 L5-1 任务书的单一提交；clean exact HEAD 在提交后复验并报告 |
| 下一动作 | 发布但不在本任务中实施 L5-1 Sandbox；真实临床、患者服务、应用 runtime 和公开生产继续 NO-GO |

### ACC-20260722-018 L5-1 Sandbox 任务发布核对

| 项目 | 结果 |
|---|---|
| 前置 | `ACC-20260722-017` accepted；独立 Reviewer 无开放 P0/P1/P2；沙盒数据/运行边界成立 |
| 结论 | **L5-1 已发布 / 待交付，尚未实施** |
| 范围 | 只允许新增 `sandbox_safety.py`、单一专项测试和 handoff；不修改 Legacy engine/review/record、配置、依赖、DB、UI 或部署 |
| 运行限制 | fixed fictitious inline fixture、fake injected evaluator、不可用 loopback env、零网络；禁止读取 `.env`、ignored `data/`，禁止启动应用/Compose/外部依赖 |
| 验收核心 | frozen strict DTO、双 digest、determinism、fail closed、limit + 1 零调用、异常无原值、1000 次资源上限、L0/全量非集成回归 |
| 状态边界 | 任务发布不是实现完成，不是 L5-2～L5-4 授权，也不是临床、生产或外部服务准入 |
| 发布身份 | 包含任务书的本次单一管理提交；执行者开始前记录 clean exact release HEAD |

### ACC-20260722-019 L5-1 第 1 次开发交付独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | release `4a2e1392aea660d08c2440113059a94103c2df3d`；delivery `d3eb7b90611b477eb837c395215473f80fe9726f`；仅含合同允许的 3 个新增文件，交付后 clean |
| RED / 开发门禁 | 真实 collection RED；专项 `10 passed`；Safety `71 passed, 3 deselected`；Ruff、mypy、L0、lock、diff、scope/tracked 通过；开发者资源 p95 `1.933 ms`、p99 `2.681 ms`、RSS `+315,392 B` |
| 独立 CI | 专项 `10 passed`；Safety `71 passed, 3 deselected`；L4.5-11 隐私 `76 passed`；L0 `131 passed`；校准后全量 `1635 passed, 362 deselected`；精确 `APP_ENV=sandbox-test` 命令只因既有 defaults 测试冲突为 `1 failed, 1634 passed, 362 deselected`，失败原样保留 |
| Reviewer | **rework required**；P0=0、P1=1、P2=2、P3=0；全过程 exact commit、read-only、worktree clean |
| P1 | adapter 只比较连续两次 evaluator 输出；返回 calls 1～2=`block`、3～4=`allow` 的 stateful evaluator 令相同 subject/bundle/command/run/trace 先 block 后 allow，subject digest 相同而 result digest 改变 |
| P2-1 | inline fixture 只有三个 slice marker，缺准入包 §5.3/§8 要求的 admission scope、provenance、source、N/A generator/构造证据、case/hash、creation role/time 和脱敏扫描记录 |
| P2-2 | 已提交 1,000 次资源测试使用 64 items 但仅 1 issue；Reviewer 的 64 items + 256 issues 探针仍通过（p95 `17.681 ms`、p99 `41.205 ms`、RSS `+655,360 B`），因此是自动化证据缺口而非已观察性能失败 |
| 结论 | **未接受 / bounded R1**；保留 `d3eb7b9`、handoff、CI 和 findings；发布 `L5-1-R1`，不得发布 L5-2 |
| 边界 | 不修改配置修复 `APP_ENV` 冲突；不读取 `.env`/ignored data；不扩大到 Runtime、HTTP、DB、Gateway、Legacy、临床或生产 |

### ACC-20260722-020 L5-1-R1 最终验收

| 项目 | 结果 |
|---|---|
| release / delivery | R1 release `53cbb9cad9bbd4630f4259409708966df4369e4d`；delivery `461487e03d6529dfacbc7f3f1ff1fe919e8633d5`；exact parent/merge-base 正确；只修改原 3 个允许文件；全程 clean |
| RED / GREEN | 原生产行为下新增 4 项真实 RED：`10 passed, 4 failed`；最终专项 `14 passed`；失败提交 `d3eb7b9` 和 R1 历史均保留 |
| P1 关闭 | arbitrary/stateful callable 在 authority 之前被拒且 calls=0；adapter 无 evaluator slot、sampling、cache、clock/random；immutable authority→bundle→subject→result digest 链由 adapter 机械解释；同 adapter/新 adapter/PYTHONHASHSEED 重放稳定 |
| P2 manifest 关闭 | strict complete manifest/scan 包含 §5.3/§8 全字段；missing/extra/provenance/case/scan 固定 `SCHEMA_INVALID`，content/manifest/dataset 篡改固定 `DIGEST_MISMATCH` |
| P2 resource 关闭 | committed true-max 为 64 items + 256 issues、20 warmup + 1,000 samples；Reviewer p95 `7.208 ms`/p99 `10.034 ms`/RSS `+290,816 B`；CI p95 `7.721 ms`/p99 `15.851 ms`/RSS `+233,472 B`；均低于原阈值 |
| 独立 Review | exact `461487e`：P0=0、P1=0、P2=0、P3=0；原三项 findings 全部关闭；size 262,144/262,145 边界、stale/new authority、无 case、digest 链、chainless fail-closed 均独立复现 |
| 独立 CI | 专项 `14 passed`；Safety `71 passed, 3 deselected`；L4.5-11 privacy `76 passed`；Ruff/mypy/L0 `131 passed`、lock/diff/scope/tracked/clean 通过；校准全量 `1639 passed, 362 deselected` |
| 精确环境差异 | 强制 `APP_ENV=sandbox-test` 全量为 `1 failed, 1638 passed, 362 deselected`，唯一为既有 `test_load_with_defaults` 断言 `local`；按合同保留，不修改范围外 config/test；只移除 `APP_ENV` 且所有 fake endpoint 不变的语义超集全绿 |
| PM 探针 | 同 exact HEAD 定向 6 项 `6 passed`；pair drift、stateful callable、manifest/digest、decision/run 分离和 true-max 全部通过；true-max p95 `7.042 ms`、p99 `8.794 ms`、RSS `+790,528 B` |
| 结论 | **通过 / accepted**；关闭 `R-L5-DET-001`、`R-L5-EVID-001`；允许项目经理发布 L5-2，但本事务不实施 L5-2 |
| 边界 | 仅个人学习、非临床、离线 synthetic sandbox；Runtime、HTTP、容器、DB、Gateway、Legacy、真实医疗/生产继续 NO-GO；G1～G6/EXT-001/EXT-002 不变 |

### ACC-20260722-021 L5-2 Sandbox 任务发布核对

| 项目 | 结果 |
|---|---|
| 前置 | L5-1/R1 accepted（`461487e`、`ACC-20260722-020`）；独立 Review 无 findings；CI/PM 探针通过；worktree clean |
| 依据 | L5 准入包 §7.4、§7.7、§8、§8.1；`DEC-20260722-015` |
| 结论 | **L5-2 已发布 / 待交付，尚未实施** |
| 范围 | 只允许新增 `sandbox_explanation.py`、单一 L5-2 专项测试和 handoff；不得修改 L5-1、配置、依赖、Legacy、Runtime、DB、Gateway、review/record/export |
| 核心边界 | 只消费 strict accepted L5-1 result；port 输入仅 result digest/decision/issue ID/rule ID/severity/allowlisted text；output 不含 decision/severity/issues；每条 text 必须 exact allowlist match |
| 失败语义 | 干预字段、无依据/错引用、重复、超 64 issues、超 8 KiB、timeout/exception/bad schema 全部只产生 fixed `explanation_unavailable`；L5-1 canonical result/digest 不变 |
| 运行限制 | inline fixed-fictitious admitted fixture；injected fake port；不可用 loopback env；禁止读取 `.env`/ignored data，禁止模型、网络、应用、容器、DB、Gateway 或外部服务 |
| 发布身份 | 包含 task/台账/风险/决策/索引的单一管理提交；开发者从其 clean exact HEAD 开始；L5-3 未发布 |

### ACC-20260722-022 L5-2 第 1 次开发交付独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | release `fdc7b3e0275b7488384cda84763f77df0df6056d`；delivery `335f7ad1f8b07535edec3420f39dea5fcef02e4c`；只含 3 个允许新增文件；Review/CI 前后 clean |
| RED / 开发门禁 | 真实 module-missing collection RED；最终专项 `15 passed`；L5-1 `14 passed`；Safety `71 passed, 3 deselected`；privacy `76 passed`；Ruff/mypy/L0/lock/diff 通过 |
| 资源 | 64 issues/64 statements、candidate `7,926 B`、result `8,176 B`、1,000 samples；开发 p95 `3.787 ms`/p99 `5.207 ms`/RSS `+188,416 B`；Reviewer p95 `4.137 ms`/p99 `5.385 ms`/RSS `+143,360 B` |
| 独立 CI | exact `335f7ad` 范围/tracked/clean；专项及回归全绿；校准全量 `1654 passed, 362 deselected`；强制 `APP_ENV=sandbox-test` 仅既有 defaults 冲突 |
| Reviewer | **rework required**；P0=0、P1=1、P2=0、P3=0；replacement independent reviewer 完成完整审查 |
| P1 | `sandbox_explanation.py` 把已校验 allowlist 的 nested entry 实例直接传给 port，并在返回后从相同实例构建 verifier text map；port-side nested mutation 可令 stale digest 后的 replacement text 得到 `attached` |
| 其余边界 | source byte non-interference、输出字段限制、reference/text verifier、fixed unavailable、64/8 KiB、资源、imports、三文件 scope 均通过 |
| 结论 | **未接受 / bounded R1**；保留 `335f7ad`、handoff、CI 和 finding；发布 L5-2-R1；L5-3 不得发布 |
| 边界 | 不修改 L5-1、配置、依赖或外部边界；真实临床/患者/公开生产继续 NO-GO |

### ACC-20260722-023 L5-2-R1 最终验收

| 项目 | 结果 |
|---|---|
| release / delivery | R1 release `b09c94ce7da4edefea1e90b7c2f7963c02903c03`；delivery `1957ad311b3997499e4f9a0e3f2dd95aa652fa9e`；exact parent/branch 正确；只修改原 3 个允许文件；Review/CI/PM 前后 clean |
| RED / GREEN | 原 `335f7ad` 行为下三个合同回归真实 RED：`3 failed, 15 passed`；最终专项 `18 passed`；第 1 次失败交付与 finding 历史完整保留 |
| P1 关闭 | verifier 在调用前保存 source/allowlist canonical bytes、digest 与 primitive invariants；request nested DTO 逐字段新建；返回后和 attach 前重验 caller source/allowlist；验证不再从 port 可见实例重建 authority |
| 独立 Review | exact `1957ad3`：P0=0、P1=0、P2=0、P3=0，原共享 nested entry P1 resolved；独立复现 identity-isolated request、snapshot 原文附着、replacement fixed unavailable 和 caller mutation rejection |
| 独立 CI | L5-2 `18 passed`；true-max `1 passed`；L5-1 `14 passed`；Safety `71 passed, 3 deselected`；privacy `76 passed`；Ruff/mypy/L0 `131 passed`、lock/diff/scope/tracked/clean 全通过 |
| 精确环境差异 | 强制 `APP_ENV=sandbox-test` 全量为 `1 failed, 1656 passed, 362 deselected`，唯一为既有 `test_load_with_defaults` 预期 `local`；只移除该变量且保持全部 fake endpoints 的校准全量为 `1657 passed, 362 deselected` |
| 资源证据 | 64 issues/64 statements、candidate `7,926 B`、result `8,176 B`、1,000 samples；Reviewer p95 `8.891 ms`/p99 `9.964 ms`/RSS `+86,016 B`；均满足原阈值 |
| PM 探针 | 同 exact HEAD 定向六项 `6 passed`；三个 R1 回归、valid attach、source byte identity 和 true-max 全通过；PM p95 `7.469 ms`/p99 `8.202 ms`/RSS `+303,104 B` |
| 结论 | **通过 / accepted**；关闭 `R-L5-EXPL-001`；允许项目经理另行发布 L5-3，但本事务不发布或实施 L5-3 |
| 边界 | 仅个人学习、非临床、离线 synthetic sandbox；Runtime、HTTP、容器、DB、Gateway、Legacy、真实医疗/生产继续 NO-GO；G1～G6/EXT-001/EXT-002 不变 |

### ACC-20260722-024 L5-3 Sandbox 任务发布核对

| 项目 | 结果 |
|---|---|
| 前置 | L5-2/R1 accepted（delivery `1957ad3`、验收提交 `7662915`、`ACC-20260722-023`）；Reviewer no findings；独立 CI/PM 探针通过；worktree clean |
| 依据 | L5 准入包 §7.1、§7.2、§7.5、§7.7、§8、§8.1；`DEC-20260722-017` |
| 结论 | **L5-3 已发布 / 待交付，尚未实施** |
| 范围 | 只允许新增 `sandbox_review.py`、单一 L5-3 专项测试和 handoff；不得修改 accepted L5-1/L5-2、配置、依赖、Runtime、Legacy 或 L5-4 |
| 核心边界 | 完整 version/digest challenge；256-bit fake nonce 明文只返回一次；full submission 先 sealed，resume command 仅含 attempt ref；store 重读 authority；atomic issued→claimed→applied |
| 必备证据 | 旧版本/全部 digest/跨 session/namespace/expiry/nonce/signature 固定拒绝；32 并发恰好一次；fake restart 不重发；append-only secret-free event；无当前 confirm review 时 completion/export blocked |
| 运行限制 | inline fixed-fictitious admitted fixture；fake clock/signer/nonce；线程安全 in-memory store；禁止 wall-clock wait、网络、文件/DB、真实 LangGraph Command、身份/签署或外部服务 |
| 状态边界 | 发布的是隔离工程状态机合同，不是 Runtime 集成、真实 reviewer/credential、L5-4、临床/生产或外部服务准入 |
| 发布身份 | 包含 task/台账/风险/决策/索引的单一 management release；执行者从其 clean exact HEAD 开始 |

### ACC-20260722-025 L5-3 第 1 次开发交付独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | release `5c54b038dff9eefc060efbdbe8a5356279b4ea7f`；delivery `99a1fb822a3963a9f324232e3be465c6835694b9`；exact parent/branch 正确；仅含合同 3 个新增文件；Review/CI 前后 clean |
| RED / 开发门禁 | 真实 module-missing collection RED（exit 2，0 items/1 error）；最终专项 `35 passed`；L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock 通过 |
| 独立 CI | exact commit/scope/tracked/clean；专项与静态全绿；校准全量 `1692 passed, 362 deselected`；强制 `APP_ENV=sandbox-test` 为 `1 failed, 1691 passed, 362 deselected` 且仅既有 defaults 冲突 |
| Reviewer | **rework required**；P0=0、P1=3、P2=1、P3=0；replacement independent correctness reviewer 完成完整审查 |
| P1-1 | stage/apply 以 `now > expires_at` 判断；恰好等于到期时间仍 staged/applied，并追加 review event |
| P1-2 | restart snapshot 只做 schema parse，不验证 event derived ref 或 event/attempt action 一致性；把已 applied `reject` event action 改为 `confirm` 后 eligibility 从 blocked 变 eligible |
| P1-3 | eligibility 只按 caller checkpoint 查询，没有 per-scope current authority；同 session/thread 发布 v8 checkpoint 后旧 v7 applied checkpoint 仍 eligible |
| P2 | create boundary 对注入的 `SandboxReviewError` bare re-raise；嵌套异常的 `__cause__` 与 `__context__` 保留，不满足 fixed chainless 合同 |
| PM 复现 | 固定 synthetic probe 独立复现四项：exact expiry `staged→applied`；event `blocked→eligible`；新 v8 pending 后旧 v7 仍 eligible；cause/context 均非空 |
| 结论 | **未接受 / bounded R1**；保留 `99a1fb8`、handoff、CI 和 findings；发布 `L5-3-R1`；L5-4 不得发布 |
| 边界 | R1 只改原三个文件；不扩到 L5-4、Runtime、DB、真实身份/签署或外部系统；真实临床/患者/公开生产继续 NO-GO |

### ACC-20260722-026 L5-3-R1 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R1 release `e3f1472d2956aa9a1e350938d28adc00e6b8d41f`；delivery `f5b7211a51418f2cd09348fc60c993576568a5b2`；exact parent/branch/scope/tracked/clean 正确 |
| RED / GREEN | 四项回归真实 RED 为 `13 failed, 35 passed`；最终专项 `48 passed`；原 35 项保持 |
| 独立 CI | L5-3 `48`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1705 passed, 362 deselected`；强制环境仅既有 defaults 冲突 |
| 原 finding | exact expiry、event-only stale ref、distinct-checkpoint current marker、nested cause/context 原复现均 resolved；32 并发仍一成功 |
| Reviewer | **rework required**；P0=0、P1=3、P2=1、P3=0 |
| P1-1 | attempt ref 只绑定 challenge ref + signed digest；同步改变 attempt/event action 并重算 event ref 后 restore 接受，reject 可变 eligible confirm |
| P1-2 | applied challenge refs 以 set 比较，允许同一 single-use challenge 恢复两个 applied attempts/events/transition sets |
| P1-3 | eligibility 在 current marker 前要求历史 checkpoint ID 唯一；同 checkpoint ID 新 interrupt 已 applied confirm 时错误 blocked |
| P2 | restore 只比较 event 与 applied transition 同时，不验证 `issued_at <= staged/applied < expires_at`；exact expiry 的重算历史可被接受 |
| 结论 | **未接受 / bounded R2**；保留 `f5b7211`、R1 CI/Review 与原失败历史；发布 L5-3-R2；L5-4 不得发布 |
| 边界 | R2 仍只改原三个文件；不实现 L5-4 stale/full recheck、Runtime、DB、真实身份/签署或外部系统 |

### ACC-20260722-027 L5-3-R2 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R2 release `aa9661200ec3d55c230ef32ec8c242c050990cf9`；delivery `03ba9104a9d79924fb4d1241429161a8d80f989e`；exact parent/branch/scope/tracked/clean 正确；仅原三个允许文件 |
| RED / GREEN | 四项回归真实 RED 为 `7 failed, 48 passed`；最终专项 `55 passed`；原 48 项保持 |
| 独立 CI | L5-3 `55`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1712 passed, 362 deselected`；强制环境 `1 failed, 1711 passed, 362 deselected`，仅既有 defaults 冲突 |
| R1 finding | coordinated attempt/event action、两个 applied attempts、复用 checkpoint current eligibility、四种 noncausal restored time 全部 resolved；原 expiry/event/current/chainless 回归继续关闭；恢复后 32 并发仍一成功 |
| Reviewer | **rework required**；P0=0、P1=1、P2=1、P3=0 |
| P1 | live stage 只查 upper expiry，不拒绝 `now < issued_at`；apply 不拒绝 `now < staged_at`，两者都能成功 mutation 后产生被自身 `snapshot()` validator 拒绝的历史 |
| P2 | restore 逐 attempt 校验时间，但未证明 cross-attempt state-machine 顺序；可接受 loser staged transition 位于 winner `review_applied` transition 之后的不可达历史 |
| 结论 | **未接受 / bounded R3**；保留 `03ba9104`、R2 CI/Review 与前两轮失败历史；发布 L5-3-R3；L5-4 不得发布 |
| 边界 | R3 仍只改原三个文件；不实现 L5-4 stale/full recheck、Runtime、DB、真实身份/签署或外部系统 |

### ACC-20260722-028 L5-3-R3 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R3 release `80e989f6b6cd3d663a8a94bd92e508ef47628748`；delivery `605f32466b71c6f6a0f1a41ece5fd3eb3d0ac12b`；exact parent/branch/scope/tracked/clean 正确；仅原三个允许文件 |
| RED / GREEN | 两项回归真实 RED 为 `2 failed, 55 passed`；最终专项 `57 passed`；原 55 项保持 |
| 独立 CI | L5-3 `57`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1714 passed, 362 deselected`；强制环境 `1 failed, 1713 passed, 362 deselected`，仅既有 defaults 冲突 |
| 原 finding | live predecessor clock、loser stage-after-applied、shared predicate、winner 连续 transitions 全部 resolved；跨 attempt clock 回拨正例、R2/R1/初始 findings 与恢复后 32 并发全部通过 |
| Reviewer | **rework required**；P0=0、P1=0、P2=1、P3=0 |
| P2 | 依次 apply A/B 后，保持 transitions 不变，反转 events、重编号 sequence 并重算 event refs；restore 错误接受 event `(B,A)` 与 `review_applied` transition `(A,B)` 的 live-unreachable 双日志顺序 |
| 结论 | **未接受 / bounded R4**；保留 `605f3246`、R3 CI/Review 与前三轮失败历史；发布 L5-3-R4；L5-4 不得发布 |
| 边界 | R4 仍只改原三个文件；不比较跨 event 时间戳，不实现 L5-4、Runtime、DB、真实身份/签署或外部系统 |

### ACC-20260722-029 L5-3-R4 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R4 release `b557a36fbf130073aa63e14ba53e984e6dac5a0b`；delivery `1f8503e0c28dd19dcc48b6e63b3e5103ce9adeda`；exact parent/branch/scope/tracked/clean 正确；仅原三个允许文件 |
| RED / GREEN | 指定回归真实 RED 为 `1 failed, 57 passed`；最终专项 `58 passed`；原 57 项保持 |
| 独立 CI | L5-3 `58`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1715 passed, 362 deselected`；强制环境 `1 failed, 1714 passed, 362 deselected`，仅既有 defaults 冲突 |
| 原 finding | event 顺序与 `review_applied` transition 顺序已精确绑定；反转 events/resequence/rederive fixed chainless reject；跨 challenge clock 回拨、R3～初始 findings 与 32 并发全部通过 |
| Reviewer | **rework required**；P0=0、P1=0、P2=1、P3=0 |
| P2 | 依次 issue A/B 后，保持 challenge/source/checkpoint 顺序不变，反转两条初始 transitions、重编号并重算 refs；restore 错误接受 issue `(A,B)` 与 initial transition `(B,A)` 的 live-unreachable history |
| 结论 | **未接受 / bounded R5**；保留 `1f8503e0`、R4 CI/Review 与前四轮失败历史；发布 L5-3-R5；L5-4 不得发布 |
| 边界 | R5 只完成有显式 sequence 的 issue/apply projections；不为 attempts/current-authorities 发明顺序，不实现 L5-4 或外部系统 |

### ACC-20260722-030 L5-3-R5 最终验收

| 项目 | 结果 |
|---|---|
| release / delivery | R5 release `ef8139dee1320860cf8c924ecf2c53de4e860925`；delivery `95be09a1b766c36eb4da411162b33a2efb4a1346`；exact parent/branch/三文件 scope/tracked/clean 正确 |
| RED / GREEN | issue/initial projection 回归在 R4 production 上真实 `1 failed, 58 passed`；R5 最终专项 `59 passed`；原 58 项保持 |
| 独立 Review | exact `95be09a1`：P0=0、P1=0、P2=0、P3=0；issue/apply 两类显式 sequence 投影完整；attempts/current keyed-unordered 正例、全部历史 findings 与恢复后 32 并发通过 |
| 独立 CI | L5-3 `59`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1716 passed, 362 deselected` |
| 精确环境差异 | 强制 `APP_ENV=sandbox-test` 全量为 `1 failed, 1715 passed, 362 deselected`，唯一为既有 `test_load_with_defaults` 默认值差异；只移除该变量且保持全部 fake endpoints 的校准全量零失败 |
| PM 复验 | exact HEAD 定向六项 `6 passed`；覆盖 issue/apply 两类投影、live/restart 时间一致性、checkpoint current 解析与 32 并发；复验后 HEAD 不变且 clean |
| 结论 | **通过 / accepted**；关闭 `R-L5-RESUME-001`；L5 进度 3/4；允许项目经理另行发布 L5-4，但本事务不发布或实施 L5-4 |
| 边界 | 仅固定虚构/合成数据、离线单元与 in-memory sandbox；Runtime、HTTP、容器、部署、DB、Gateway、Legacy、真实临床/患者/公开生产继续 NO-GO；L6 未开始 |

### ACC-20260722-031 L5-4 Sandbox 任务发布核对

| 项目 | 结果 |
|---|---|
| 前置 | L5-3/R5 accepted（delivery `95be09a1`、验收提交 `d2ab8e75c33f18c2ac8d805ff037365c8ab0d363`、`ACC-20260722-030`）；Reviewer no findings；独立 CI/PM 通过；worktree clean |
| 依据 | L5 准入包 §7.1、§7.2、§7.6、§7.7、§8、§8.1；`DEC-20260722-024` |
| 结论 | **L5-4 已发布 / 待交付，尚未实施** |
| 范围 | 只允许新增 `sandbox_recheck.py`、单一 L5-4 专项测试和 handoff；不得修改 accepted L5-1/L5-2/L5-3、配置、依赖、Runtime、Legacy 或 PM 台账 |
| 核心边界 | 当前 applied `modify_fixture` → revision/state 精确 +1 → append-only 全产物失效 → accepted L5-1 完整重检 → BLOCK 或 private L5-3 新 challenge；新 confirm 前 completion blocked |
| 原子/恢复 | 单一 outer lock、copy-on-write private L5-3 snapshot、accepted 后失败仍保持旧 authority 失效、strict combined snapshot/restart、32 并发单次 accepted、idempotent retry 不重发 nonce |
| 禁止 | 无真实 LangGraph/Runtime/HTTP/容器/部署/DB/Gateway/外部服务，无 record/done/export；固定虚构/合成 inline fixture，`UV_OFFLINE=1` |
| 发布身份 | task/台账/风险/验收/决策/索引单一管理提交；开发者从 clean exact HEAD 开始；L6 未开始 |

### ACC-20260722-032 L5-4 第 1 次开发交付独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | release `7ee8286ce56c406a392468d21d66a565155ce9f0`；delivery `d5b8f0e775aac4c9d2ac89d6c9b8c6991a2e186a`；exact parent/branch 正确；仅含合同 3 个新增文件；Review/CI 前后 clean |
| RED / 开发门禁 | 真实 module-missing collection RED（exit 2，0 items/1 error）；最终专项 `32 passed`；L5-1～L5-3 合并 `91 passed`；Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 通过 |
| 独立 CI | exact `d5b8f0e`：L5-4 `32`、L5-3 `59`、L5-2 `18`、L5-1 `14` 全绿；校准全量 `1748 passed, 362 deselected`；强制 `APP_ENV=sandbox-test` 为 `1 failed, 1747 passed, 362 deselected` 且仅既有 defaults 差异；scope/tracked/clean 通过 |
| Reviewer | **rework required**；P0=0、P1=0、P2=2、P3=0；独立 reviewer 在 exact commit 完成只读复核 |
| P2-1 | combined snapshot 未逐 revision 验证 namespace/session/thread/review schema 的继承与 challenge 关联；重派生相关 refs 后，terminal blocked schema 漂移与三 revision 链中间 namespace 漂移仍可 restore |
| P2-2 | ALLOW setup 捕获 source/challenge 初始化异常后，又在保护范围外重复构建 review source；source build failure 会泄露动态异常，且已接受 modification 的 revision/run/invalidation/receipt 全部未提交 |
| PM 复验 | 主路径六项定向探针 `6 passed`，证实当前已测路径；不能覆盖独立 reviewer 新复现的两项缺口，故不据此接受 |
| 结论 | **未接受 / bounded R1**；保留 `d5b8f0e`、真实 RED/GREEN、独立 CI 和 findings；发布 `L5-4-R1`；L5 保持 3/4，L6 不得开始 |
| 边界 | R1 只改原三个 L5-4 交付文件；不改 accepted L5-1/L5-2/L5-3、配置、依赖、PM 台账或任务书，不扩大到 Runtime/外部系统；真实临床/患者/公开生产继续 NO-GO |

### ACC-20260722-033 L5-4-R1 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R1 release `9382fc7e411d90530cb4abb93479272d8655b7dd`；delivery `8b345b9cb807b64409a118d8c18d8ce7b8d41835`；exact parent/branch/三文件 scope/tracked/clean 正确 |
| RED / GREEN | 两组回归在原 production 上真实 `3 failed`；最终专项 `35 passed`；原 32 项保持；terminal schema/middle identity 与 source-build atomicity 两项初始 P2 已关闭 |
| 独立 CI | L5-4 `35`、L5-3 `59`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff 全绿；校准全量 `1751 passed, 362 deselected`；强制环境仅既有 defaults 差异 |
| Reviewer | **rework required**；P0=0、P1=0、P2=2、P3=0；原两项 finding 均 resolved，但完整性根因族仍有新缺口 |
| P2-1 | restore 仅重建 command/digest，未复用 live 完整 prevalidation；重派生 refs 后可接受 subject/bundle adapter 不一致的 resultless terminal，或 child 复用 parent checkpoint/interrupt |
| P2-2 | initial `modify_applied` 未要求 event 属于 exact challenge 且 challenge 已 applied；terminal 只清 outer challenge ref，不拒绝 private snapshot 仍保留同 revision source/challenge/current marker |
| PM 复验 | exact R1 HEAD 六项 `6 passed`；覆盖原 findings、正常 review 与 32 并发，但不覆盖新复现，故不据此接受 |
| 结论 | **未接受 / bounded R2**；保留 `d5b8f0e`、`8b345b9`、两轮 Review/CI/RED/GREEN；发布 `L5-4-R2`；L5 保持 3/4，L6 不得开始 |
| 边界 | R2 只改原三个 L5-4 文件；不改 accepted L5-1/L5-2/L5-3、配置、依赖、PM 台账或任务书，不扩大到 Runtime/外部系统 |

### ACC-20260722-034 L5-4-R2 独立验收

| 项目 | 结果 |
|---|---|
| release / delivery | R2 release `84c8f6459ac086457dfb9218575ac2360c4068af`；delivery `23a561a5c972cd9fb0103fb7d39ebbe7ad841cbb`；exact parent/branch/三文件 scope/tracked/clean 正确 |
| RED / GREEN | 四类回归在 R1 production 上真实 `4 failed`；最终专项 `39 passed`；原 35 项保持；shared command predicate 与 terminal retained-authority 两项 R1 根因已关闭 |
| 独立 CI | L5-4 `39`、L5-3 `59`、L5-2 `18`、L5-1 `14`、Safety `71/3 deselected`、privacy `76`、L0 `131`、Ruff/mypy/lock/AST/diff/scope/tracked/clean 全绿；校准全量 `1755 passed, 362 deselected`；强制环境 `1 failed, 1754 passed, 362 deselected` 且仅既有 defaults 差异 |
| Reviewer | **rework required**；P0=0、P1=0、P2=1、P3=0；shared live/restore command predicate 与 terminal authority absence 已 resolved |
| P2 | combined restore 在 only-initial 状态只验证 outer challenge 是 historical applied modify，未要求它等于 private snapshot 的 same-scope current marker；先保留历史 applied challenge、再发布新的 same-scope pending challenge，outer initial 仍指向历史 challenge 时会被接受 |
| PM 复验 | exact R2 HEAD 六项定向探针 `6 passed`；证明当前 39 项与既有主路径，但不覆盖 Reviewer 新的 current marker 复现，故不据此接受 |
| 结论 | **未接受 / bounded R3**；保留初始/R1/R2 三个失败 deliveries、全部 RED/GREEN、Review 与 CI 证据；发布 `L5-4-R3`；L5 保持 3/4，L6 不得开始 |
| 边界 | R3 只改原三个 L5-4 文件，以统一 same-scope issue projection/current marker 不变量收敛；不改 accepted L5-1/L5-2/L5-3，不扩大到 Runtime、HTTP、DB、Gateway、Legacy、外部系统或 L6 |

## 新记录模板

```markdown
### ACC-YYYYMMDD-NNN 任务名

| 项目 | 结果 |
|---|---|
| 基线/提交 |  |
| 结论 | 通过 / 未通过 / 阻塞 |
| 范围核对 |  |
| 专项测试 |  |
| 全量门禁 |  |
| 独立复审 |  |
| 阻塞变化 |  |
| 下一动作 |  |
```
