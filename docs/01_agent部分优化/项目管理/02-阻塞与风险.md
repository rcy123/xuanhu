# 阻塞与风险

## 活跃工程阻塞

当前无活跃工程阻塞。

AR-B-001～AR-B-030 的详细发现和关闭证据保存在旧进度表历史归档中。

## 本轮关闭的工程阻塞

| 编号 | 关闭证据 | 结论 |
|---|---|---|
| AR-B-031 | `140262d` 入口等长投影；`5ada262` Runtime 最终门禁；`ada23c77` matcher 线性性能；两轮组合独立 Review、独立 CI、PM 跨层探针；`ACC-20260722-014` | 已关闭；L4.5-11 accepted；外部 EXT-001/EXT-002 不受影响 |

## 外部门禁

| 编号 | 门禁 | 责任角色 | 状态 | 工程侧动作 |
|---|---|---|---|---|
| EXT-001 | `triage-raw-text-precheck.v1` 临床红旗规则人工审定 | 具名临床专业人员 | 未关闭 | 保持开关默认关闭，不代签 |
| EXT-002 | L5 产品/医疗安全/隐私/法务/伦理准入 | 对应具名负责人 | 未关闭 | L5 保持 NO-GO |

## 当前风险

| 编号 | 严重度 | 风险 | 缓解 |
|---|---|---|---|
| R-PM-001 | P1 | 文档状态分散、被 `.gitignore` 忽略，可能再次出现本地批准但 Git 无证据 | 项目管理中心和 Agent handoff 默认纳入 Git；提交前检查 tracked paths |
| R-PRIV-001 | P1 | 继续按 Unicode/转义样例追加正则会重回打地鼠循环 | 有限威胁模型、单一入口投影、最终不可绕过门禁和停止条件 |
| R-SCOPE-001 | P1 | 隐私修复顺手修改 Domain、临床规则、Legacy 或 L5 | 每个任务维护允许/禁止文件和触发即停止条件 |

## 本轮关闭的工程风险

| 编号 | 关闭证据 | 结论 |
|---|---|---|
| R-MATCH-001 | `140262d`；专项 `53 passed`；独立 Review 复验同起点候选、68 个单一切分和连续多 `B`；PM 探针通过 | 已关闭；组合验收继续运行专项回归 |
| R-FAILCLOSED-001 | `140262d`；matcher/mask 故障注入；固定消息、无 cause/context、无日志原值；独立 Review 与 CI 通过 | 已关闭；Runtime 门禁仍须独立证明自己的固定失败码 |
| R-GATE-001 | `5ada262`；专项 `19 passed`；拒绝时 observed/plain/request 和 Gateway method-property 读取均为 0；安全 Intake `max_requests=1`；非 Intake 不读 content；独立 Review/CI、PM 探针通过 | 已关闭；组合验收继续复验纵深防御和非 Intake/Legacy 不变量 |
| R-GROUND-001 | `140262d` 与 `ada23c77`；逐 raw-coordinate 等长、68 个跨 message 切分、DTO/grounding offset、20,000 raw-range 差分、两轮组合 Review | 已关闭；第一轮不等长方案失败史保留 |
| R-PERF-001 | `ada23c77`；独立/PM 单条约 `4 ms`、batch 约 `33 ms`；parent RED `>22 ms/message`；O(n) 审查、60,000+ 差分样本、独立 CI 与最终 Reviewer no findings | 已关闭；宿主持久争用作为运行环境残余风险，不放宽阈值 |

## 关闭要求

阻塞关闭必须同时记录：修复提交、专项结果、全量结果、独立复审结论、残余风险和对应验收记录编号。仅有“测试通过”或交付声明不能关闭 P0/P1。
