# 当前状态

> 更新日期：2026-07-22
> 更新人：Codex（工程项目经理）

## 项目快照

| 项目 | 当前事实 |
|---|---|
| 当前阶段 | L5 个人学习工程沙盒准入完成；真实临床/公开生产轨道继续 NO-GO |
| 当前任务 | `L5-1` 确定性 SafetyRuleEngine adapter（Sandbox）：已发布 / 待交付，尚未实施 |
| 当前工程基线 | L5-PREP-0 发布基线 `ad8cbf038cb3e0a18c9ce40f88d5ee235c04a4d7`；已接受生产代码仍为 `ada23c77`；管理 release 为包含本状态事务的提交 |
| LangGraph | L0～L4 功能历史验收完成；L4.5-11-1、L4.5-11-2 与 R2 均 accepted；AR-B-031 已关闭 |
| Legacy | 代码行为不变；不因本次范围调整获得真实临床或公开生产授权 |
| WebUI / 后端开关 | 两个 LangGraph 公共开关继续默认关闭 |
| L5 | 个人学习离线工程沙盒 `sandbox_scope_satisfied`；实现 0%；真实临床、患者服务和公开生产继续 NO-GO |
| 沙盒运行边界 | 只准入离线单元级 L5-1 adapter；现有应用 runtime、HTTP/E2E、容器、部署、数据导入和外部连接继续 NO-GO |
| 工作区要求 | 每个发布、交付和验收节点结束时保持 clean |

## 唯一下一动作

执行 bounded 的 [L5-1 Sandbox](../../dev-handoff/agent-refactor-l5-1-sandbox-task.md)：只新增离线、immutable、fail-closed adapter 与单一专项测试。执行者必须从包含任务书的 clean release HEAD 开始，先红后绿；不得启动应用/容器、读取 `.env`/ignored `data/`、连接数据库/网关或扩展 L5-2～L5-4。

## 当前工程门禁

- AR-B-031 已关闭；L4.5-11 已 accepted；
- R-PERF-001 已以单条约 `4 ms`、8-message batch 约 `33 ms` 和独立等价复审关闭；
- L5-PREP-0 已 accepted；独立 Reviewer 最终 P0/P1/P2/P3 全为 0；
- L5-1 Sandbox 已发布但尚未实施；
- L4.5-02 / EXT-001 和 L5 G1～G6 / EXT-002 均未获得专业批准；
- 它们对当前个人学习、非临床、仅合成数据沙盒标记为 `deferred_for_clinical_use` 或 `external_approval_required`，不构成沙盒文档/工程准备的前置条件；
- 任何真实数据、患者/医师/机构工作流、诊疗用途、公开服务、商业使用、临床评价或人体研究将自动重新打开适用专业门禁。

## 当前禁止事项

- 不直接执行 B133 回退前 v1 A～F；
- 不恢复已回退的 10,000+ 行 Context 复杂实现；
- L5-1 只能修改任务书明确允许的一个生产模块、一个专项测试和一个 handoff；不得顺手实施 L5-2～L5-4；
- 不修改 Legacy 默认行为、RAG、UI、临床红旗规则或 Domain 事实语义；
- 不使用真实患者/个人数据，不连接外部临床系统，不部署或公开提供医疗服务；
- 不把 `sandbox_scope_satisfied` 或 AI 工程验收写成专业签署、临床批准或法律结论。
