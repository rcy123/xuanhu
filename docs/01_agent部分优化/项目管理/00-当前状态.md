# 当前状态

> 更新日期：2026-07-28
> 更新人：Codex（工程项目经理）

## 项目快照

| 项目 | 当前事实 |
|---|---|
| 当前阶段 | L9 API/UI、控制面、L5-PROD、L6-PROD 与 Recovery **本地工程 accepted**；真实发布、L9-4 与临床轨道继续 NO-GO |
| 当前任务 | 产品闭环、本地 PG/Redis/checkpoint 全量门禁与三个 Codex 子 agent 独立复验已完成；等待真实 full 稳定窗口、Legacy 排空、运维回滚及 L7/L8 产品/外部门禁。 |
| 当前工程基线 | L5/L6 `e8e0973`；L7 `da604d7` / `ACC-20260728-061`；L8 `9e402c6` / `ACC-20260728-062`；L9 本地工程 `ACC-20260728-068` / `DEC-20260728-063` |
| LangGraph | L0～L4 功能历史验收完成；L4.5-11-1、L4.5-11-2 与 R2 均 accepted；AR-B-031 已关闭 |
| Legacy | 代码行为不变；不因本次范围调整获得真实临床或公开生产授权 |
| WebUI / 后端开关 | WebUI 不再持有运行时开关；后端 default 仍为 Legacy，public/product-ready 默认 false，rollout phase 默认 `legacy` |
| L5 | L5-SBX re-accepted；L5-PROD Safety/Review Runtime/DB/HTTP/interrupt 产品接线与 Codex 独立复验 accepted，真实发布仍 NO-GO |
| L6 | L6-SBX re-accepted；L6-PROD deterministic Record/API/DB 产品接线与 Codex 独立复验 accepted，真实发布仍 NO-GO |
| L6-1 | 历史单项 `ACC-20260723-049` 保留；当前 exact coordinator capability 与 v2 record 以 `ACC-20260727-058` 为准 |
| L6-2 | 历史 R1 `ACC-20260724-050R1` 保留；raw-snapshot authority 合同已被 `DEC-20260727-051` supersede |
| L6-3 | 历史交付保留；`ACC-20260724-051` 仅为发布核对；当前 live-authority-bound Store 以 `ACC-20260727-058` 为准 |
| L6-4 | 历史单项交付保留；当前以 `e8e0973` 的 L6-SBX v2 组合验收为准；真实临床/公开生产继续 NO-GO |
| 运行边界 | 应用 Runtime、HTTP、专用测试 PG/Redis 与 checkpoint 已获本地工程 acceptance；真实部署、真实数据、外部连接、临床/公开使用继续 NO-GO |
| L8 | L8-SBX **accepted / engineering complete**；Episode/metrics、安全、故障恢复和离线评估合同通过；L8-PROD、真实模型、生产 shadow 和外部门禁继续 NO-GO |
| 工作区要求 | 每个发布、交付和验收节点结束时保持 scoped clean；用户排除目录、环境密钥与凭据保持未读取、未修改、未提交 |

## 唯一下一动作

L9 本地工程和三个 Codex 子 agent 独立复验已经完成。唯一下一动作是由有权环境与
责任角色执行真实 full 稳定窗口、Legacy open=0 排空、运维回滚，以及 L7/L8
产品/外部门禁。L9-4 在这些真实发布证据齐备前保持禁止。

真实临床、患者服务、公开生产、商业/机构接入和人体研究继续 NO-GO。默认 Legacy、
public/product-ready=false、rollout phase=legacy 均未改变。

## 2026-07-27 权威覆盖说明

- `ACC-20260727-054` 记录 L7 全量撤回；`ACC-20260727-055` 记录 L5/L6 历史 acceptance reopen；`ACC-20260727-056` 以 `45acf54` 重新验收 L5-SBX/L6-SBX。
- `ACC-20260727-057` 在撤权探针失败后再次打开 `ACC-20260727-056`；`ACC-20260727-058` 以 `e8e0973` 完成实时权限与状态完整性复验收。当前授权只以 `ACC-20260727-058` / `DEC-20260727-054` 为准。
- 旧 L6-2 raw snapshot 多态输入、L6-3 Store-only 无 authority 写入合同被 `DEC-20260727-051` 正式 supersede；以下 2026-07-24 段落仅作历史证据，不再代表当前接口。

## 当前工程门禁

- 三个 Codex 子 agent 分别覆盖 L5、L6 与 Recovery/rollout；初审与 R1 的 REWORK
  findings 均按限定范围修复，最终三路均 `ACCEPT`，
  P0/P1/P2/P3=`0/0/0/0`，见 `ACC-20260728-066`～`068`。
- 最终非 integration：`2381 passed, 398 deselected in 168.87s`；专用
  loopback/tmpfs PostgreSQL/Redis integration：
  `397 passed, 1 xfailed, 2381 deselected in 668.11s`。
- 前端 `24 files / 187 tests passed in 53.61s`，typecheck、lint、build 通过；
  全仓 Ruff、`mypy app scripts`（175 个源码文件）、lock、diff check 通过；
  Alembic 单一 head `20260728_0014`。
- 最终门禁保留完整校准史：曾出现 `D:\tmp` CWD/env 噪声与 performance 环境泄漏；
  另发现一个真实 `advance` preflight-order 问题，经一行修复和定向
  `4 passed` 后，随后 `final2` 非 integration 与 integration 全绿。
- 稳定窗口现在只以 durable rollout phase audit 的当前 `full_entered_at` 和
  `--deployment-id` 计算；runtime-switch 年龄、canary 时长或旧 full 窗口均不能
  冒充连续 full 稳定窗口。
- 默认 Legacy、public/product-ready=false、phase=legacy 均未改变；本地工程
  acceptance 不授权真实切流、L9-4、Legacy 删除或临床/公开发布。

- L5/L6 组合专项：`338 passed in 79.07s`；全量非 integration：`1963 passed, 362 deselected in 116.07s`。
- 全仓 Ruff、`mypy app scripts`（159 个源码文件、0 错误）、`uv lock --check`、`git diff --check` 全通过。
- 全仓 `mypy app tests` 仍有 917 个错误 / 65 个文件，均在测试目录，作为既有测试注解债务保留；当前结论不等于 full mypy 通过或债务清零。
- 前端 lint、typecheck 通过；`23 test files / 171 tests passed`。
- L7-SBX exact `da604d7`：专项 `186 passed`；十文件 L5/L6/L7 组合 `524 passed`；非 integration `2149 passed, 362 deselected`；PM 对抗矩阵 `42 passed`；scoped Ruff/format/mypy、lock、diff 通过；独立 Claude Code Reviewer `ACCEPT`，P0/P1/P2/P3=`0/0/0/0`。
- L8-SBX exact `9e402c6`：四专项 `55/42/57/9 passed`；组合 `163 passed`；最终非 integration `2312 passed, 362 deselected`；scoped Ruff/format/mypy、lock、diff 通过；终端 Claude Code Reviewer `ACCEPT`，P0/P1/P2/P3=`0/0/0/5`。
- L8 全量校准曾有 1 次 L3 随机 UUID/隐私扫描偶发失败；单项 `1 passed`、L3 文件 `56 passed`、随后全量复跑全绿。该事实记录在 L8 handoff，不归因于 L8，也不宣称既有全仓零偶发风险。
- 全仓 `ruff check .` 与 `mypy app scripts` 通过；全仓 `ruff format --check .` 仍有 128 个既有文件待格式化，不属于 L7 diff，当前不宣称全仓 format clean。
- 历史 SBX 的 DB integration 缺口已由本轮专用测试基础设施关闭；该结论只覆盖临时隔离测试服务，不等于真实部署或生产数据验收。

## 当前禁止事项

- 不恢复、cherry-pick 或复用旧 L7 发布/实现；新 L7-SBX 必须重新发布任务合同。
- 不把 configured-pattern scan 写成全面 PII、真实合成来源或隐私合规证明。
- L5/L6 产品 Runtime、HTTP、DB、Doctor Review、Record 与本地 E2E 只能引用本轮产品交付证据；不得从 SBX 或本地测试扩大到真实数据、临床/公开使用。
- 不用尚未配置的 DB integration、全仓既有 mypy 债务或历史错误验收编号伪造当前门禁结论。

## L6-2 复审与 R1 闭环记录（2026-07-24）

- L6-2 第 1 次交付 `d8dfa44` 原始 16 项测试、L6-1+L5 回归 `204 passed`、校准全量 `1813 passed, 366 deselected`、Ruff/mypy/lock 全部通过；
- PM 在原始测试通过后进行 10 项 adversarial 独立探针，发现 2 项 FAIL（A9 bytes 快照被拒绝、A6 经分析为 fixture 约束不构成 finding）；
- P2-1：verifier 对 bytes/str 快照双重序列化，`canonical_review_bytes` 非幂等，对已序列化 bytes 再序列化产生 JSON 引号字符串，`model_validate_json` 报 `Input should be an object` → 被 `except` 吞成 `False`；方向 fail-closed，是功能缺陷非安全漏洞；
- P2-2：assembler 与 verifier 共享同一段双序列化代码，缺陷在两个模块传播；L6-1/L6-2 测试均未覆盖 bytes/str 输入路径，形成盲区；
- 处置（撤回）：撤回 `ACC-20260723-050` accepted；撤销 `DEC-20260723-044` 的 L6-3 发布；发布 `L6-2-R1`；L6-3 未发布；
- 修复（R1）：提取共享 helper `_parse_recheck_snapshot`，显式 isinstance 分支处理 instance/bytes/str/dict/其他，不依赖 `canonical_review_bytes` 非幂等行为；verifier 和 assembler 共用 helper；
- 验收（R1）：L6-2-R1 专项 `32 passed`、回归 `422 passed`、全量 `1829 passed`、PM 22/22 探针全通过；两个 P2 已关闭；L6-2 标记 accepted（通过 R1）；L6-3 改进版任务书重新发布；真实临床/公开生产继续 NO-GO。

## 历史工程门禁（截至 2026-07-24，仅作证据）

- AR-B-031 已关闭；L4.5-11 已 accepted；
- R-PERF-001 已以单条约 `4 ms`、8-message batch 约 `33 ms` 和独立等价复审关闭；
- L5-PREP-0 已 accepted；独立 Reviewer 最终 P0/P1/P2/P3 全为 0；
- L5-1 第 1 次交付 `d3eb7b9` 的 P1=1、P2=2 失败历史保留；
- R1 `461487e` 以 immutable declarative authority、完整 admitted manifest 和 64 items + 256 issues 资源门禁关闭全部 findings；
- 独立 R1 Reviewer P0/P1/P2/P3 全为 0；独立 CI、PM 定向探针、L0、静态、lock、scope/tracked/clean 通过；L5-1 **accepted**（`ACC-20260722-020`）；
- 强制 `APP_ENV=sandbox-test` 的全量仍保留唯一既有 defaults 测试冲突；只移除 `APP_ENV` 且保持全部 fake endpoints 的校准全量 `1639 passed, 362 deselected`；
- L5-2 第 1 次交付 `335f7ad`：独立 CI 校准通过；独立 Reviewer P0=0、P1=1、P2=0、P3=0，结论 rework（`ACC-20260722-022`）；
- P1：adapter 把 verifier 已校验的 allowlist entry 实例直接放入 port request，返回后又从同一实例构建 text map；port 可改变共享 nested entry，使已校验 digest 失效并让 replacement text attached；
- L5-2-R1 `1957ad3`：真实 RED `3 failed, 15 passed`，最终专项 `18 passed`；copy isolation、primitive verifier snapshot 和 source/allowlist 双重 post-call revalidation 已实现；
- 独立 R1 Reviewer P0/P1/P2/P3 全为 0，原共享 nested entry P1 resolved；独立 CI 的 L5-1/Safety/privacy/L0/静态/lock/scope/tracked/clean 全通过；
- 强制 `APP_ENV=sandbox-test` 全量为 `1 failed, 1656 passed, 362 deselected` 且仅既有 defaults 冲突；校准全量 `1657 passed, 362 deselected`；PM 六项定向探针通过；
- L5-2 **accepted**（`ACC-20260722-023`），R-L5-EXPL-001 关闭；
- L5-3 已发布（`ACC-20260722-024`、`DEC-20260722-017`）：只允许离线 in-memory/fake state machine；真实 LangGraph Command、Runtime、HTTP、DB、身份/签署与外部服务继续禁止；
- L5-3 第 1 次交付 `99a1fb8`：开发与独立 CI 专项 `35 passed`、校准全量 `1692 passed, 362 deselected`，scope/静态/clean 通过；
- 独立 Reviewer P0=0、P1=3、P2=1、P3=0，结论 rework（`ACC-20260722-025`）：exact expiry 仍 applied、restart event action 可漂移、旧 authority 仍 eligible、异常链未完全归一化；
- PM 固定 synthetic 探针复现 `expires_at` staged/applied、event `blocked→eligible`、新 v8 后旧 v7 eligible、cause/context 均残留；L5-3-R1 已发布，L5-4 继续禁止发布；
- L5-3-R1 `f5b7211`：真实 RED `13 failed, 35 passed`，最终专项 `48 passed`；独立 CI 校准全量 `1705 passed, 362 deselected`；原 expiry/distinct-checkpoint/chainless findings 关闭；
- R1 独立 Reviewer P0=0、P1=3、P2=1、P3=0（`ACC-20260722-026`）：coordinated attempt/event action、两个 applied attempts、复用 checkpoint ID 的 current eligibility、restore 时间因果仍未闭合；
- L5-3-R2 `03ba9104`：真实 RED `7 failed, 48 passed`，最终专项 `55 passed`；独立 CI 校准全量 `1712 passed, 362 deselected`，强制环境仅既有 defaults 冲突；R1 四项 finding 全部关闭；
- R2 独立 Reviewer P0=0、P1=1、P2=1、P3=0（`ACC-20260722-027`）：live stage/apply 缺 predecessor lower bound，可生成自身 restore 拒绝的历史；restore 仍接受 challenge applied 后才 stage loser attempt 的不可达序列；
- L5-3-R3 `605f3246`：真实 RED `2 failed, 55 passed`，最终专项 `57 passed`；独立 CI 校准全量 `1714 passed, 362 deselected`，强制环境仅既有 defaults 冲突；R2 两项因果 finding 全部关闭；
- R3 独立 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-028`）：反转两个 events、重编号并重算 refs 后 restore 仍接受，与保持原序的 `review_applied` transitions 形成 live-unreachable append order；
- L5-3-R4 `1f8503e0`：真实 RED `1 failed, 57 passed`，最终专项 `58 passed`；独立 CI 校准全量 `1715 passed, 362 deselected`，强制环境仅既有 defaults 冲突；event/apply transition 顺序 finding 已关闭；
- R4 独立 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-029`）：反转 A/B 初始 transitions、重编号并重算 refs 后 restore 仍接受，与 challenge `issue_sequence` 形成 live-unreachable issue order；
- L5-3-R5 `95be09a1`：真实 RED `1 failed, 58 passed`，最终专项 `59 passed`；完整显式 sequence 投影与 keyed-unordered 边界已审计；
- R5 独立 Reviewer P0/P1/P2/P3 全为 0；独立 CI 校准全量 `1716 passed, 362 deselected`，强制环境仅既有 defaults 冲突；PM 六项定向复验通过；
- L5-3 **accepted**（`ACC-20260722-030`）；`R-L5-RESUME-001` 关闭；验收提交本身未发布或实施 L5-4；
- L5-4 已由独立管理提交发布（`ACC-20260722-031`、`DEC-20260722-024`）：private L5-1/L5-3 composition、append-only invalidation、full recheck 与 exact-current completion probe；尚未实施或 accepted；
- L5-4 第 1 次 delivery `d5b8f0e`：真实 collection RED；最终专项 `32 passed`；独立 CI 校准全量 `1748 passed, 362 deselected`，强制环境仅既有 defaults 差异；范围/静态/clean 通过；
- 独立 Reviewer P0=0、P1=0、P2=2、P3=0（`ACC-20260722-032`）：重派生 refs 后可伪造中间 revision namespace 或 terminal review schema；source build failure 会绕过 accepted modification 的 invalidation 提交并泄露动态异常；
- L5-4 **未接受**；失败 delivery 与全部 CI/Review 证据保留；`L5-4-R1` 已发布，L5 仍为 3/4，L6 未开始；
- L5-4-R1 `8b345b9`：真实 RED `3 failed`，最终专项 `35 passed`；独立 CI 校准全量 `1751 passed, 362 deselected`，强制环境仅既有 defaults 差异；原 terminal schema/middle identity 与 source-build atomicity 两项 P2 已关闭；
- R1 独立 Reviewer P0=0、P1=0、P2=2、P3=0（`ACC-20260722-033`）：restore 未复用 live 完整 command 预验证；初始 exact event/state 与 terminal absence 不变量未闭合；
- L5-4-R1 **未接受**；初始/R1 deliveries 与全部证据保留；`L5-4-R2` 已发布，L5 仍为 3/4，L6 未开始；
- L5-4-R2 `23a561a`：真实 RED `4 failed`，最终专项 `39 passed`；独立 CI 校准全量 `1755 passed, 362 deselected`；shared full command predicate 与 terminal retained-authority 已关闭；
- R2 独立 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-034`）：combined restore 仍未要求 only-initial applied-modify challenge 等于 private current marker；
- L5-4-R2 **未接受**；前三个 deliveries 与全部证据保留；`L5-4-R3` 已发布，L5 仍为 3/4，L6 未开始；
- L5-4-R3 `b7cbbff`：真实 RED `3 failed`，最终专项 `43 passed`；校准全量 `1759 passed, 362 deselected`；same-scope issue suffix 与三类 expected current 已关闭；
- R3 独立 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-035`）：合法 other-scope source 穿插后，current review applied confirm 仍被 completion 的宽 source 查询错误阻断；PM 独立复现 `applied / blocked`；
- L5-4-R3 **未接受**；四个失败 deliveries 与全部证据保留；`L5-4-R4` 已发布，L5 仍为 3/4，L6 未开始；
- L5-4-R4 `c71832b`：真实 RED `1 failed`，最终专项 `44 passed`；独立 CI 校准全量 `1760 passed, 362 deselected`，强制环境仅既有 defaults 差异；completion exact-current source P2 已关闭；
- R4 独立 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-036`）：terminal absence 仍按全 store subject 匹配，错误拒绝合法 other-scope authority；PM 独立复现 `review_setup_failed / restore rejected`；
- 同一 scope qualification 根因连续两轮出现，停止逐调用点字段补丁；`L5-4-R5` 作为 bounded 架构收敛已发布；L5 仍为 3/4，L6 未开始；
- L5-4-R5 `847076e`：关系矩阵真实 RED `2 failed, 1 passed`、GREEN `3 passed`、完整专项 `47 passed`；finite authority qualification matrix 与 helper ownership 已收敛；
- R5 独立 Reviewer P0/P1/P2/P3 全为 0；独立 CI forced full `1 failed, 1762 passed, 362 deselected` 且仅既有 defaults 差异，calibrated full `1763 passed, 362 deselected`；PM 六项 `6 passed`；
- L5-4 与 R5 **accepted**（`ACC-20260722-037`）；`R-L5-RECHECK-001` 已关闭；L5 进度 4/4，但最终组合 Reviewer/CI/PM 尚未执行，整体不得提前标记完成；
- 最终组合 exact `be33ffc`：独立 CI 组合 `138 passed`、calibrated full `1763 passed, 362 deselected`，PM 跨层 `13 passed`；但最终 Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-038`）；
- 唯一 P2：current `review_required` child 与 private challenge 可同步切换 review schema version、重派生全部 refs 后被 restore 接受；PM 独立复现 parent v1 / child v2 `restore=accepted`；
- `ACC-037` 作为历史单项 acceptance 保留，但 L5-4 与 `R-L5-RECHECK-001` 已由最终组合 finding 重新打开；`L5-4-R6` 已发布，L5 当前 3/4，L6 未开始；
- L5-4-R6 `f6b790d`：真实 RED `2 failed, 1 passed`、GREEN 定向 `3 passed`、完整专项 `50 passed`；统一 parent/child review schema inheritance 已移到所有 child 状态共享路径；
- R6 独立 Reviewer P0/P1/P2/P3 全为 0；独立 CI calibrated full `1766 passed, 362 deselected`，forced full 仅既有 defaults 差异；PM 定向 `3 passed`；
- L5-4/R6 **accepted**（`ACC-20260722-039`、`DEC-20260722-032`）；`R-L5-RECHECK-001` 再次关闭；L5 恢复 4/4 individually accepted，但整体仍须新的最终组合验收；L6 未开始；
- 最终组合第 2 轮 exact `4ffbaff`：独立 CI 组合 `141 passed`、calibrated full `1766 passed, 362 deselected`，PM 跨层 `13 passed`；Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260722-040`）；
- 唯一 P2：真实 initial applied review snapshot 可把固定 v1 整体协调改为 v2并重派生全部记录引用，L5-3 store 与 L5-4 coordinator 均接受；R6 的 child==parent 在无 child 时不会执行；
- `ACC-030/039` 历史单项结论保留，但 L5-3/L5-4 与 `R-L5-RESUME-001` / `R-L5-RECHECK-001` 已重新打开；`L5-3/4-R7` 已发布，L5 当前 2/4，L6 未开始；
- L5-3/4-R7 `d3ee3ce`：两层完整重派生真实 RED `4 failed`、GREEN `4 passed`；L5-3/L5-4 专项 `62/51 passed`；shared fixed schema guard 与 live issue 共用同一常量；
- R7 独立 Reviewer P0/P1/P2/P3 全为 0；独立 CI forced `1 failed, 1769 passed, 362 deselected` 且仅既有 defaults 差异，calibrated `1770 passed, 362 deselected`；PM 六项 `6 passed`；
- L5-3/L5-4 与 R7 **accepted**（`ACC-20260723-041`、`DEC-20260723-034`）；两个工程风险再次关闭；L5 恢复 4/4 individually accepted，但整体仍须 final R3；L6 未开始；
- final R3 exact `78c9b13`：独立 CI 组合 `145 passed`、calibrated full `1770 passed, 362 deselected`，PM 跨层 `13 + 4 passed`；Reviewer P0=0、P1=0、P2=1、P3=0（`ACC-20260723-042`）；
- 唯一 P2：live proof 对三个测试身份 identifier 有相同长度/格式约束，但 persisted sealed attempt/event 未复用；协调修改 applied snapshot 并重派生 refs 后，shared restore 接受 live DTO 明确拒绝的值；
- `ACC-030/039/041` 历史单项结论保留，但 L5-3/L5-4 与两个工程风险重新打开；`L5-3/4-R8` 已发布，L5 当前 2/4，L6 未开始；
- L5-3/4-R8 `6119654`：production 未改时三字段×三类非法值与共享结构真实 RED 为 L5-3 `10 failed`，L5-4 composition 为 `3 failed`；单一 `_ReviewTestIdentifier` 修复后定向 `10/3 passed`，完整专项 `72/54 passed`；
- R8 独立 Reviewer P0/P1/P2/P3 全为 0，并独立复跑 L5-3/L5-4 `126 passed`；独立 CI 组合 `158 passed`、forced `1 failed, 1782 passed, 362 deselected` 且仅既有 defaults 差异、calibrated `1783 passed, 362 deselected`；PM `13 passed` + 9/9 求值后共享类型探针通过；
- L5-3/L5-4 与 R8 **accepted**（`ACC-20260723-043`、`DEC-20260723-036`）；两个工程风险再次关闭；L5 恢复 4/4 individually accepted，但整体仍须全新的 final R4；L6 未开始；
- final R4 exact `14c0496`：独立 CI 组合 `158 passed`、calibrated full `1783 passed, 362 deselected`，PM 跨层 `25 passed` + R8 `13 passed`；Reviewer P0=0、P1=1、P2=0、P3=0（`ACC-20260723-044`）；
- 唯一 P1：attempt/event action 可同步改变并完整重派生 private refs，而原 signed payload digest 保持；restore 只比较 attempt/event 彼此相等，未从 challenge authority 重算，因而 blocked/eligible 可翻转并传播到 L5-4；
- `ACC-030/039/041/043` 历史单项结论保留，但 L5-3/L5-4 与两个工程风险重新打开；`L5-3/4-R9` 已发布，L5 当前 2/4，L6 未开始；
- L5-3/4-R9 `259a563`：完整重派生真实 RED 为 L5-3 `4 failed`、L5-4 `2 failed`；无密钥 persisted-challenge digest helper 修复后 L5-3/L5-4 `75/56 passed`；
- R9 独立 CI 组合 `163 passed`、forced `1 failed, 1787 passed, 362 deselected` 且仅既有 defaults 差异、calibrated `1788 passed, 362 deselected`；PM R9 `6 + 5 passed`；但 Reviewer P0=0、P1=1、P2=0、P3=0（`ACC-20260723-045`）；
- R9 P1：action、digest 与全部 refs 可同步重算；helper 只用 snapshot 自身字段自校验，shared/L5-4 restore 均接受并翻转 eligibility。R9 未接受；两个工程风险保持 reopened；R10 已发布，L5 仍 2/4；
- L5-3/4-R10 `e985d2e`：production 零改时 L5-3 `10 failed, 1 passed`、L5-4 `6 failed`；bounded/repr-hidden proof + injected verifier 修复后 R10 `11/6`、完整 L5-3/L5-4 `84/60 passed`；
- R10 独立 Reviewer P0/P1/P2/P3 全为 0，并额外核验 scheme/key/reviewer 协调变化、多 attempts、candidate 残留与 sealed REJECT；独立 CI 组合 `176`、forced `1 failed, 1800 passed, 362 deselected` 且仅既有 defaults 差异、calibrated `1801 passed, 362 deselected`；PM `17 passed`；
- L5-3/L5-4 与 R10 **accepted**（`ACC-20260723-046`、`DEC-20260723-039`）；两个工程风险再次关闭；L5 恢复 4/4 individually accepted，但整体仍须全新 final R5；L6 未开始；
- final R5 冻结 exact `c052c501`：全新独立 Reviewer P0/P1/P2/P3 全为 0，四层 `176 passed`，额外多 attempts、proof/verifier、candidate 零残留及零网络/子进程探针通过；
- final R5 独立 CI：R7/R8/R9/R10 `4/13/6/17`，Safety `71/3 deselected + 18`、privacy `76`、Runtime/Legacy/public `57/13 deselected`、flag `10`、AST `10/134 deselected`、L0 `131`、Ruff/mypy/lock 全通过；forced 仅既有 defaults 差异，calibrated full `1801 passed, 362 deselected`；
- 根 PM 在同一冻结点独立复验四层 `176 passed`、R10 `17 passed` 与最终语义矩阵 `36 passed`；41/41 required paths tracked，scope/ancestry/diff/exact/clean 全通过；
- L5 个人学习离线工程沙盒 **accepted / engineering complete**（`ACC-20260723-047`、`DEC-20260723-040`）；失败史与全部 reopen 记录保留，L6 已开始（L6-1/L6-2 accepted、L6-3 改进版任务书已发布）；
- L4.5-02 / EXT-001 和 L5 G1～G6 / EXT-002 均未获得专业批准；
- 它们对当前个人学习、非临床、仅合成数据沙盒标记为 `deferred_for_clinical_use` 或 `external_approval_required`，不构成沙盒文档/工程准备的前置条件；
- 任何真实数据、患者/医师/机构工作流、诊疗用途、公开服务、商业使用、临床评价或人体研究将自动重新打开适用专业门禁。

## 历史禁止事项（截至 2026-07-24，仅作证据）

- 不直接执行 B133 回退前 v1 A～F；
- 不恢复已回退的 10,000+ 行 Context 复杂实现；
- L5 关闭不授权自动进入 L6；未来若出现 P0/P1/P2，必须追加 reopen 记录并发布 bounded rework，不得改写本次或历史结论；
- 不修改 Legacy 默认行为、RAG、UI、临床红旗规则或 Domain 事实语义；
- 不使用真实患者/个人数据，不连接外部临床系统，不部署或公开提供医疗服务；
- 不把 `sandbox_scope_satisfied` 或 AI 工程验收写成专业签署、临床批准或法律结论。
