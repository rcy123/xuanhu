# 决策记录

> 架构和流程决策只追加；决策被替代时新增一条记录并引用旧编号。

## DEC-20260721-001 Python 统一为 3.12

- 决策：本地开发、CI、mypy、Ruff、验收脚本和锁文件只支持 Python 3.12。
- 原因：仓库只有一个实际 `.venv`，双版本门禁增加维护成本且不对应双运行时。
- 提交：`b97c9f9`。

## DEC-20260721-002 统一测试根目录

- 决策：L0-1 文档契约测试归并到 `tests/`，pytest 的 `testpaths` 只保留 `tests`。
- 原因：`test_agent/` 只有一个正式回归文件，不是第二套测试框架。
- 提交：`b97c9f9`。

## DEC-20260721-003 接受 B133 回退

- 决策：不恢复未提交的 B1～B133 复杂 Context 返工。
- 原因：该路径形成无限 Unicode/转义/跨分片样例追加，缺乏稳定停止条件。
- 影响：旧 v1 A～F 只作为历史参考。

## DEC-20260721-004 L4.5-11 从当前小基线重启

- 决策：先执行文档限定的 L4.5-11-0，再决定 v2 实现任务。
- 基线事实：`context.py` 210 行，`UserMessageProjection` 不存在，Context 专项 9 项。
- 不变量：原始持久消息与模型输入投影分离；EvidenceSpan 必须保持可验证；L5 NO-GO。

## DEC-20260721-005 Codex 接管工程 PM

- 决策：Codex 维护任务发布、范围、工程验收、阻塞和证据。
- 边界：不代签专业审批，自审不算独立 reviewer。
- 提交：`6a7b8c4`。

## DEC-20260721-006 项目管理文档分账

- 决策：旧 8.5 万字进度表只读归档；当前状态、任务、阻塞、验收、决策分别维护。
- 原因：避免同一文件混合实时状态与历史流水，并消除多处复制导致的状态冲突。
- 兼容：保留 `Agent优化任务进度表.md` 原路径作为入口。
