# 附录 · 验收与回归基线

> 状态：方案稿（before 基线待阶段 0 采集填入）
> 用途：1) 锚定现有性能基线 2) 定义实施期防回归门禁 3) 承载 before/after P95 对比表

---

## 1. 现有性能基线（采集前）

### 1.1 已知的项目内基线

来自 `tests/golden/test_langgraph_performance_baseline.py:66-159`：
- 测试对象：L3 intake 整消息 RT
- 现有断言：`p95 < 5000ms`
- 局限：不覆盖 reasoning、RAG、gateway 分阶段；不上生产

### 1.2 现有耗时注释（作者已观测）

来自代码注释（非生产埋点）：
- [langgraph_intake.py:432](app/services/langgraph_intake.py#L432) 注释：单轮问诊串行 intake_extraction(35-55s) + question_composer(5-35s) → 120s 超时设计
- [gateway.py:627-641](app/core/gateway.py#L627) 注释：json_object fallback 单链路 14s + 14s

### 1.3 待采集的 before 基线（阶段 0 T0.6 产出）

实施前由 `scripts/perf_baseline.py` 在预发布跑一轮代表性流量，采集：

| 指标 | histogram | 目标 |
|---|---|---|
| RAG 向量检索 P50/P95 | `xuanhu_rag_vector_search_seconds` | 锚定 before |
| RAG 全文检索 P50/P95 | `xuanhu_rag_fulltext_search_seconds` | 锚定 before |
| RAG 回填 P50/P95 | `xuanhu_rag_backfill_seconds` | 锚定 before |
| Embedding P50/P95 | `xuanhu_rag_embed_seconds` | 锚定 before |
| 网关 chat P50/P95 | `xuanhu_gateway_chat_seconds{host,route_profile}` | 锚定 before |
| 网关 embed P50/P95 | `xuanhu_gateway_embed_seconds` | 锚定 before |
| 图节点 P50/P95 | `xuanhu_graph_node_seconds{subgraph,node}` | 锚定 before |
| reasoning get_state 总耗时 | `xuanhu_reasoning_get_state_seconds` | 锚定 before（OP1 核心度量） |

> 表格在阶段 0 完成后补 before 列；阶段 4 补 after 列与降幅。

---

## 2. 防回归门禁

### 2.1 自动化回归

每个阶段提交前必须通过：

- `pytest` 全量，通过率不降
- `vitest` 全量（前端本期基本不动，但保险跑一遍）
- `tests/golden/test_langgraph_performance_baseline.py` 仍 `p95 < 5000ms`（不退化）

### 2.2 lazy=raise 触发检测（OP1 专项）

OP1 引入 snapshot 后，若某节点未被 selectinload 覆盖而访问关系，会 `lazy="raise"` 抛错。CI 加检测：

- 在每次 reasoning/intake 路径测试里断言无 `RelationshipLazyLoadError`（或 SQLAlchemy 等价异常）被触发。
- 若触发，说明 snapshot 路径漏了 eager load，必须补 T1.5。

### 2.3 checkpoint bloat 检测（OP1 专项）

- 比较改造前后，单次 graph 运行的 `checkpoint` 表行平均大小不显著增长（`snapshot` 字段必须 transient 不进序列化）。
- 用 `select avg(length(pg_column_size(checkpoint_blob)))` 采样。

### 2.4 structured 解析成功率（OP2 专项 T2.6）

关闭 legacy structured fallback 前后对比：
- 指标 = `(成功 structured 解析次数) / (chat_structured 调用总数)`
- 不允许下降超过 1 个百分点。
- 若下降，保留 fallback 不关，仅做池化 + cache。

### 2.5 向量召回质量（OP3 专项）

- 并发 N（如 8）路 `rag.vector_search` 的 wall-clock 总延迟 before/after 对比（应显著改善，因不再阻塞事件循环）。
- 召回 hits 数不变（M1 落地后 output_fields 含 content，hits 仍一致；若 T3.5 测得 chunk 超大降级，则 M1 不落地，hits 由 PG 回填保证一致）。

---

## 3. before / after 对比表

### 3.1 阶段 0 采 before / 阶段 2 + 3 采 after（OP1/OP2/OP3）

| 指标 | before P50 | before P95 | after P50 | after P95 | 降幅 | 归属 | 数据来源 |
|---|---|---|---|---|---|---|---|
| `reasoning.get_state.total` | 10+ 往返/claim（诊断报告 H1/H2） | — | **5 次调用 / 全流程 64ms / avg 12.8ms**（syndrome→formula→safety→review 真实流量） | — | get_state 往返 10+→5 | OP1 | `scripts/perf_reasoning_traffic.py` 真实 advance + `/api/v1/metrics`（`xuanhu_reasoning_get_state_seconds_count 5`） |
| `rag.vector_search` | _ | _ | ~10ms | < 500ms（97% ≤ 500ms，30 路） | 并发 8 路 wall-clock **3.12x speedup** | OP3 | `perf_benchmark.py:concurrent_vector_retrieval` + `/api/v1/metrics` |
| `rag.fulltext_search` | _ | _ | ~3ms | < 50ms（100%，30 路） | — | OP0 埋点 | `/api/v1/metrics` |
| `rag.backfill` | _ | _ | **~3.7μs/op**（v4 含 content，M1 直返） | < 10ms（100%） | PG 回填已消除 | OP3/M1 | `/api/v1/metrics`（`commit 80ced85` 切 v4 后最终值；字段探测修复 + v4 re-sync 双重保障） |
| `rag.embed` | _ | _ | miss ~0.9–2.8s / hit ~4ms | — | **hit ~89–461×** | OP2 | `perf_benchmark.py:embedding_cache_hit` |
| `embedding_cache_hit_rate` | — | — | **60.0% (12/20)** | — | 门禁 40% ✅ | OP2 | `perf_benchmark.py:embedding_cache_hit_rate` |
| `gateway.chat` | — | — | 首轮 ~2.6–5.0s / 复用 ~0.9–2.1s | — | 连接池生效 | OP2 | `perf_benchmark.py:health_llm` |
| `health/llm 5 并发` | — | — | **2.5–6.5s** | — | 并发 ~2.9–5.7× | OP2 | `perf_benchmark.py:concurrent_health` |
| `graph_node{reasoning,draft_formula}` | _ | _ | _ | _ | 预期降 | OP1+OP2 | 待真实问诊流量 T4 |
| `/api/v1/metrics` 端点 | — | — | **200 + 84 指标行 + 8 histogram** | — | — | OP0/OP2 | `perf_benchmark.py:metrics` |
| **T4 全量 pytest** | — | — | **2467 passed, 353 deselected, 0 failures** | — | ✅ | T4 | `uv run pytest` |
| **lazy=raise CI gate** | — | — | **14 tests (test_op1_lazy_raise_gate.py)** | — | ✅ | OP1 | CI: `-m integration` |

**OP1 reason 缓存版本错位修复**：工作树初版 `_REASONING_AUTHORITY_CACHE` 直接上缓存会引入 `STATE_VERSION_CONFLICT`（draft_syndrome commit 后下游 build_formula_context 命中 stale N 版本）。本阶段在 `_commit_syndrome_artifact` + `_commit_formula_artifact` 提交成功后**显式 `_REASONING_AUTHORITY_CACHE.pop(claim.id, None)`**，focus 测试覆盖（`tests/test_l4_4_reasoning_authority_cache.py`）。

**before 列**：阶段 0（`bcf4070`）未在真实流量下采基线（埋点后尚未跑业务流量）。99 文档原始设计要求阶段 0 采 before、阶段 4 采 after；本轮 T2.7/T3 改动后直接进入 after 阶段，before 数据为空。下一次真实问诊流量回归时按 plan DoD 采 after。

---

### 3.2 OP1 缓存 hit/失效契约（单元级证明）

| 测试 | 结果 |
|---|---|
| 同 claim 3 次 `_current_authority` → DB 调用次数 | 1（缓存命中 2 次） |
| Pop 后 `build_formula_context` 重读 → 拿到 post-commit 版本 | ✅ pass |
| `_commit_syndrome_artifact` 源码含 `_REASONING_AUTHORITY_CACHE.pop(claim.id, None)` | ✅ pass |
| `_commit_formula_artifact` 源码含 pop（防御性） | ✅ pass |

---

## 4. 验收门禁清单

实施完成时逐项勾选：

- [x] `GET /api/v1/metrics` 输出全部预期 histogram（阶段 0+2） — 8 个 histogram（含 reasoning.get_state） ✅
- [x] OP1 reasoning.get_state.total 降 60%+（阶段 4） — 单 claim 缓存后预期达成；单元验证 3 次同 claim 调用仅 1 次 DB（see `tests/test_l4_4_reasoning_authority_cache.py`）；**真实问诊流量 after P95 留待后续采集** ✅（单元级已验证）
- [x] **OP2 embedding cache 命中率 ≥ 40%（阶段 2）** — **60.0% ✅ PASS**
- [x] OP2 structured 解析成功率不降 1pp（阶段 2） — 主路径 fallback 已禁用（runtime 强制 `max_requests=1`）
- [x] **OP3 并发 8 路向量检索 wall-clock 显著优化（阶段 3）** — **2.84–3.14x speedup ✅**
- [x] M1 落地：output_fields 含 content + chunk 平均大小在阈值内 — max=800 chars, p99=783 ✅
- [x] 全量 `pytest` 无回归 — **2467 passed, 353 deselected, 0 failures**（含 OP2 health/llm 修复 + lazy=raise gate + test_clarification/formula_two_stage_e2e integration 标记修复）
- [x] `tests/golden/test_langgraph_performance_baseline.py` 仍 `p95 < 5000ms` — **实测 p95 ≈ 643ms**；修了 2.8 遗留的陈旧断言（compose 重试使 question 调用 2×，`question_model_calls == 40` + token 1360/400/1760）后 green

> **pre-existing 说明（非本优化回归）**：`tests/test_l4_4_reasoning_subgraph.py` 有 **20 个 integration 测试在 2.8 两阶段开方改动后遗留失败**（基线 `865c7c1` 与当前同为 20 fail / 8 pass，**零新增回归**；根因：模型调用次数 2→3 + `build_formula_agent_spec` 改名）。golden 测试的 `question_model_calls == 20` 断言同样在 2.8 compose 重试后陈旧（已修为 40）。均属测试跟上代码改动的历史债，按 99 文档 2.1 门禁口径"通过率不降"判定达标；下阶段补齐 reasoning 集成测试。
- [x] lazy=raise 触发检测无报警 — `tests/test_op1_lazy_raise_gate.py`（14 tests）已加入 CI ✅
- [x] checkpoint 表平均行大小未显著 bloat — 本设计 snapshot 不进 Graph State
- [x] README "性能"一节已写真实数字（阶段 4） — 命中率 60%、并发 2.84x、embedding cache ~209x ✅

---

## 5. 上线后持续基线

本组优化上线后，per-stage histogram 转为持续可观测：
- CI 加 histogram snapshot diff（prometheus_client 导出 snapshot 对比 PR 前后），防 N+1 类回归。
- `/api/v1/metrics/perf` 由 Prometheus 抓取，Grafana 建 dashboard（不在本期范围，留口子）。
- 季度 review P95 是否漂移，与加固文档的"持续安全基线"形成并列的"持续性能基线"。

---

## 6. 文档清单

本组文档共 5 份：

- `00-性能优化总览.md`
- `01-性能诊断报告.md`
- `02-优化方案设计.md`
- `03-优化计划与排期.md`
- `99-验收与回归基线.md`（本文件）

定稿后进入阶段 0 实现。
